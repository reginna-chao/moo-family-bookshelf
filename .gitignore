# Dependencies
node_modules/
.pnpm-store/
# nested lockfiles — workspace uses the root pnpm-lock.yaml only
extension/pnpm-lock.yaml
worker/pnpm-lock.yaml

# Build output
dist/
dist-firefox/
dist-firefox-amo/
dist-firefox-direct/
dist-firefox-updates.json
web-ext-artifacts/
web-ext-artifacts-amo/
web-ext-artifacts-direct/
*.zip
*.xpi

# Environment & secrets
.env
.env.*
# Tracked on purpose — VITE_* public endpoint URLs only, inlined into the built bundle.
# Never secrets. If one becomes local-only, remove its negate line here too.
!/.env.example
!/.env.production
!/.env.remote
.dev.vars
!.dev.vars.example

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Cloudflare
.wrangler/
wrangler.local.toml

# Test
coverage/
playwright-report/
test-results/
.verify-selectors-profile/

# Claude
.claude/settings.json
.claude/settings.local.json

# Skill archive
.skill-archive/

# Local Readmoo DOM snapshots — real page dumps containing personal data
# (account nickname, owned book list). Keep them local, never commit.
docs/resource/

# 更新紀錄

所有使用者可見的變更都會記錄在這裡。

---

## v1.0.0（2026-04-07）

首次公開發布。

### 家庭書櫃（核心功能）

- 在讀墨網頁中直接開啟「家庭書櫃」Dialog，不需離開閱讀頁面
- 一鍵建立家庭，透過同步碼邀請家人加入
- 瀏覽家庭成員開放的書籍，支援依成員篩選
- 書名與作者即時搜尋
- 書籍分類篩選（依讀墨主題分類）
- 書櫃有新書時顯示紅點通知

### 個人書櫃管理

- 所有書籍預設不開放，由你自行選擇要分享的書
- 逐本切換開放/關閉，也可多選批次操作
- 支援已封存書籍的同步與篩選
- 變更後需手動儲存才會同步，不會意外洩露

### 家庭管理

- 查看家庭成員列表（標示管理者）
- 管理者可移除成員、轉移管理權
- 離開家庭後，其他成員立即無法看到你的書
- 個人開放設定不因離開家庭而消失，換家庭不用重新設定

### 安全與隱私

- 安全存取控制：所有資料透過 HTTPS 傳輸，以認證 Token 控管存取權限
- 不收集個人識別資訊，不需要額外帳號
- 不使用 Cookie，不嵌入追蹤器

### PWA 行動版

- 手機瀏覽器可透過 PWA 網頁使用家庭書櫃
- 支援 QR Code 快速登入（從 Extension 掃碼）
- 支援手動輸入同步碼 + Email 登入
- 可選的登入驗證保護（PIN 碼、圖形鎖、OTP）
- 個人書櫃開放設定（需先從 Extension 同步過書單）
- 支援安裝至手機桌面

### 自訂後端

- 支援自建 Cloudflare Worker 後端，完全掌控資料
- 同步碼自動帶入 API 端點（`@host`），家人不需額外設定
- Extension 與 PWA 均支援自訂 API 端點

### 其他

- 可編輯顯示名稱（不影響讀墨帳號）
- 登出時可選擇記住同步碼，方便重新登入
- 重新安裝 Extension 時自動恢復家庭資料
- 問題回報連結（GitHub / Plurk / Discord）

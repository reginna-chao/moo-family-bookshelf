# 更新紀錄

所有使用者可見的變更都會記錄在這裡。最上方的「未釋出」是已合併但還沒發布新版本的變更，會在下一次發布時併入新版本的條目。

---

## 未釋出

### 功能新增

- 每個人的已完成借閱紀錄從最近 20 筆增加到 50 筆。
  - **自己架設伺服器的人**：伺服器也要更新，否則畫面寫 50 筆，實際仍只保留 20 筆。使用官方伺服器的人不受影響。

### 介面調整

- 展開借閱分頁的歷史紀錄時，畫面上會寫出保留的筆數。

### 開發者體驗

- 自己架設伺服器的人請先把環境升到 Node.js 22 以上，Node.js 20 執行 wrangler 指令會直接失敗。
  - 環境需求列在自行架設說明（`worker/DEPLOY.md`）。

---

## v1.7.1（2026-09-17）

### 問題修正

- 擴充功能的家庭書櫃現在也會把家人後來才分享的書標成「更新」，和手機版一致。打開家庭書櫃分頁就算看過，標記會在 24 小時後自動消失。
- 已經離開或被移除的成員不能再把借閱紀錄標成已歸還，只有還在家庭裡的借閱雙方可以。
- 書封空白的書現在也能申請借閱了。申請失敗時畫面會說明原因（例如已有待處理的申請、對方關閉了借閱功能、連線失敗），先前按了沒有任何反應。

### 功能新增

- 擴充功能在建立或加入家庭的畫面會標出目前使用的自訂伺服器位址，讓你知道資料會送到哪裡。使用官方伺服器時不會顯示。

### 效能改善

- 防止有人瘋狂連線的每分鐘次數上限換了一套更省資源的做法，能用的次數完全沒變（依操作不同，每分鐘 60／30／10／3 次）。唯一看得到的差別：被擋下時畫面上的倒數都會從 60 秒開始，先前顯示的是該分鐘剩下的秒數。
  - **自己架設伺服器的人**：設定要改一下，做法在自行架設說明（`worker/DEPLOY.md`）的第 5 步。不改也能繼續運作，會沿用原本的計次方式。使用官方伺服器的人不受影響。
- 每個人自己申請的借閱，會保留最近 20 筆已完成紀錄（已歸還、已拒絕、已取消），更早的自動清掉；家人的申請各自計算，不會擠掉你的。待處理與出借中的紀錄不論幾筆都完整保留。
- 同時待處理的借閱申請每人最多 20 筆，超過時要先取消或等對方回覆才能再送新的。一般使用不會遇到。
- 有人離開家庭時，他向家人借書的已完成紀錄會一起刪除，家人向他借書的紀錄則會留著。還借在外面、還沒歸還的也會留著，直到有人標記歸還。
- 有人離開家庭時，你和他之間還在等待回覆的借閱申請會直接取消。
- 借閱分頁的載入不再隨著紀錄變多而變慢。

### 安全與隱私

- 借閱紀錄只會送到借閱雙方的裝置，跟這筆借閱無關的家人不會再收到。畫面本來就只顯示自己的紀錄，日常使用感覺不到差別。
- 家庭書櫃與公開書櫃裡的書名連結只會連到 Readmoo（`readmoo.com`／`readmoo.tw` 及其子網域），不是的話會拿掉連結、只顯示書名。
- 對「伺服器回傳怪資料」的防護擴大到家庭成員列表、成員設定、借閱提醒與家庭書櫃的書單，格式不對的項目會被忽略。使用官方伺服器的人不受影響，日常使用感覺不到差別。

---

## v1.7.0（2026-08-28）

### 問題修正

- **修正讀墨改版後擴充功能完全不能用的問題**：讀墨的書櫃換了網址（`next.readmoo.com`），舊版因此一本書都抓不到。現在新舊網址都支援，還沒換過去的帳號也能照常使用。
- 在電腦上重設 PIN 或圖形驗證後，可以馬上在手機登入，不用等鎖定時間過完。
- 修正用驗證碼登入時一定會失敗的問題。
- 修正公開書櫃在快到期的最後一分鐘儲存書單或改設定會失敗的問題。
- 「關閉公開分享」失敗時會如實顯示錯誤，不會再讓你以為連結撤銷了、其實還打得開。
- 在一台裝置關閉分享後，另一台裝置不會再把它重新打開。
- 修正 Firefox 上伺服器位址有時沒被記住、下次開啟又連回預設伺服器的問題。
- 借閱狀態顯示異常時不會再讓整個畫面出錯，那筆紀錄會顯示成「狀態未知」。
- 手機版遇到網路不穩不會再直接把你登出，會保留登入狀態並重試。
- 修正「已儲存」這類提示偶爾被上一次的提示蓋掉或提早消失的問題。
- 被移出家庭、家庭已解散、或人數已滿而無法加入時，擴充功能會在畫面上說明原因，不再無聲跳回一開始的引導畫面。
- 公開書櫃設定的錯誤提示全面中文化。標題或到期日儲存失敗時會保留你的輸入並提供「重試儲存」按鈕，不再無聲丟掉你的修改。

### 功能新增

- 家庭設定新增「解除移除限制」：管理者不小心移除成員後，按一下就能讓對方馬上重新加入，不必等 6 小時。這個動作只解除限制，對方仍要自己用同步碼加入。
- 被鎖定或操作太頻繁時，畫面會直接顯示還要等幾秒並即時倒數。

### 安全與隱私

- 有設定 PIN、圖形或驗證碼的人，別人已經無法單憑你的 Email 幫你的帳號建立家庭，也查不到你的同步碼。沒設定驗證的人行為不變。
  - **請更新擴充功能／PWA**：擴充功能會自動更新，通常幾小時內完成，想快一點可以重新啟動瀏覽器；手機版重新整理頁面、看到更新提示按確定即可。沒更新的話，有設定驗證的人會在建立家庭時失敗，已加入家庭的日常操作則不受影響。
- 別人猜錯你的 PIN 不會再鎖住你，只會鎖住猜錯的那一方。
- 同一個帳號每小時最多 10 次驗證失敗。只有猜錯才計入，所以就算有人把你帳號的次數用完，你自己輸入正確的 PIN、圖形或驗證碼一樣照常通過。
- 登入時查詢「我在哪個家庭」每分鐘最多 3 次，短時間反覆重試會被暫時擋下並顯示還要等幾秒。正常登入不會遇到。
- 公開書櫃、驗證設定與家庭設定的變更，各自加上每小時最多 30 次的上限。一般使用碰不到，超過時該動作會暫時失敗，稍候即可恢復。
- 關閉分享或重設連結後，舊連結一定打不開了；設有到期日的書櫃同樣適用，過期後一律顯示「找不到或已過期」。
- 家庭管理者更換伺服器時，其他成員會先看到目前與即將切換的完整位址，同意後才會切換。選擇「暫不切換」會被記住，在位址再次變動前不會重複詢問。
- 手機掃到帶有自訂伺服器的 QR Code 時會先顯示位址、等你按「確認並加入」，在你確認之前不會向那台伺服器送出任何連線。官方伺服器的 QR Code 不受影響，掃了還是一鍵完成。
- 如果那個位址本身有問題（例如網址裡夾帶帳號密碼，或看起來連往 A 網站、實際連往 B 網站），畫面會改為顯示警告，按下加入也會直接中止。
- 加入家庭失敗時，裝置會退回原本使用的伺服器；離開家庭或被移出時則還原成官方伺服器。
- 登入憑證過期後跳出的「需要驗證」視窗，現在也會顯示即將連線的伺服器位址。
- **手機版可能會請你重新輸入一次同步碼**：先前存過不合規伺服器位址的手機（例如網址中夾帶帳號密碼），為安全起見會被視為尚未登入。這跟你的讀墨帳號密碼無關，重新輸入同步碼即可恢復，之後不會再出現。
- 管理者移除成員現在會真的生效：被移除的人 6 小時內無法重新加入同一個家庭，手動輸入同步碼、掃 QR Code 與背景自動重連都一樣。自願離開家庭不受影響，隨時可以再加入。
  - **請更新擴充功能／PWA**：舊版不認得這個回應，會在背景一直重試又不說原因，6 小時後甚至可能自動重新加入。更新方式同上。
- 被移除的人會即時知道並登出：擴充功能回到引導畫面，手機顯示「你已被家庭管理者移出，已為你登出」。
- 建立借閱請求時，書封網址必須是 Readmoo 的網址（`readmoo.com`／`readmoo.tw` 及其子網域），其他會被拒絕，家人看書封時不會把自己的連線資訊送給第三方網站。先前已存在的異常書封會顯示為空白書封。
- 加強對「伺服器回傳怪資料」的防護：如果你的家庭改用自訂伺服器，而那台伺服器傳回格式不對或帶有惡意內容的資料，擴充功能與手機版現在會直接忽略或改用安全的預設值顯示。使用官方伺服器的人不受影響，日常使用也感覺不到差別。
- 設定家庭自訂伺服器時，常見的內網 IP 位址（例如 10.x、192.168.x 開頭）會被擋下來，本機開發用的 127.0.0.1 除外，IPv6 位址則一律不接受。填網域名稱不在檢查範圍內，指向內網的網域仍然存得進去。

---

## v1.6.1（2026-07-25）

### 介面調整

- 對話框互動元件補上一致的 hover / focus 樣式。

### 安全與穩定性

- 擴充功能更新或重新載入後，舊分頁的對話框不再出現「Extension context invalidated」錯誤，改以預設值安全降級。

---

## v1.6.0（2026-07-09）

### 功能新增

- 家庭書櫃新增「我的最愛」：可標記喜歡的書並切換「只看最愛」，僅影響自己的檢視，家人不會知道你標記了哪些書。
- 書櫃排序新增降冪（Z→A）選項，書名與作者皆可反向排序。
- 篩選 / 排序選單改為「圖示 + 文字」樣式，分頁加上圖示，並優化手機版版面與標題呈現。
- 對話框支援手機版響應式版面（含 Firefox 行動版）。
- 書籍排序下拉選單支援鍵盤操作（方向鍵 / Enter / Esc）。

### 問題修正

- 修正桌面版對話框在主畫面的高度異常。
- 修正 Firefox 上 familyId 有時無法保存的問題。
- 修正 Firefox 擴充功能的資料蒐集權限宣告（附加元件商店上架合規）。

### 安全與穩定性

- 強化家庭「重新加入 / 復原」驗證：有設定 PIN / 圖形驗證的使用者，在新裝置重新加入或復原時會被要求輸入驗證，避免他人冒用帳號；同時強化後端輸入驗證。
- 修正公開書櫃設定：在標題輸入後隨即關閉視窗時，不再送出未完成的變更。
- 更新後端相依套件（hono），修補安全告警。

---

## v1.5.0（2026-06-18）

### 功能新增

- 家庭書櫃新增「隱藏書籍」：可把不想看到的書隱藏，僅影響自己的檢視（家人不受影響），可隨時取消隱藏，並能切換檢視已隱藏的書。
- 推出 Firefox 版擴充功能，支援 Firefox 桌面版與 Firefox for Android™：可從 Firefox 附加元件商店安裝，或下載簽章版 `.xpi` 自行安裝（含自動更新）。

---

## v1.4.0（2026-06-11）

### 功能新增

- 家庭書櫃支援 Grid / Row 兩種顯示模式，偏好自動記憶。
- Extension 浮動圖示可自選大小（小 / 中 / 大）。
- 個人書櫃與家庭書櫃新增書籍排序選項（書名、作者、預設順序）。
- 書櫃自動同步頻率可自訂，開啟 Dialog 不再自動捲動書庫。
- PWA 借閱流程新增手動借出引導，提示使用者自行從讀墨完成借書操作。
- 邀請連結新增歡迎訊息與加入步驟說明，方便新成員快速上手。

### 效能改善

- 書單同步改為差異上傳（PATCH API），大幅減少網路流量。

### 問題修正

- 修正 PWA 表單欄位與按鈕高度不一致的問題。

---

## v1.3.0（2026-05-29）

### 功能新增

- 借閱流程簡化：移除手動輸入讀墨成員名稱的步驟，系統自動從家庭成員清單取得。
- 官網 footer 新增「問題回報」入口。

### 效能改善

- 大量藏書效能優化：200 本以上書籍的載入、篩選與搜尋速度大幅提升，虛擬捲動與 memo 減少不必要渲染。
- 儲存路徑效能優化：儲存分享設定時僅重繪異動項目，避免整頁重新渲染。

### 介面調整

- 家庭書櫃書籍封面放大為 120×180，更容易辨識書籍。

### 問題修正

- 修正顯示名稱在家庭紀錄、個人紀錄與 Extension 快取之間未同步的問題。
- 修正 PWA 多項 UI 問題（底部列對齊、捲動行為）與 CORS preflight 設計。

### 開發者體驗

- 新增 dev-only 互動式 API 文件（Swagger UI）：`/api/_docs` 可直接測試所有 API，僅 dev 環境開啟。

---

## v1.2.1（2026-05-06）

### 新功能

- 「問題回報」新增 Google 表單入口：點擊圖示直接開啟回報表單，並自動帶入目前 Extension 版本，使用者不需手動填寫。

### 問題修正

- QR Code 連結改為按需產生：開啟 Dialog 不再立即向後端索取 token，僅在使用者點擊「產生連結」時才發起 API 呼叫，避免不必要的請求。
- 修正 Dialog 載入畫面 footer 底部出現的留白。
- 載入畫面新增 spinner 動畫，提供清楚的載入中提示。
- 修正 PWA 個人書櫃批次操作工具列無法停留在視窗底部的問題；工具列現在會固定在底部 Tab Bar 正上方並支援 iOS 安全區。

---

## v1.2.0（2026-05-03）

### 公開書櫃分享

- 個人書櫃新增「公開分享」按鈕：可產生獨立網址，分享給家庭外的朋友瀏覽。
- 公開書櫃內容 = 你已開放的書（與家庭分享同一組），更新書單時公開頁面自動同步。
- 自訂書櫃標題與過期時間（7 / 30 / 60 / 90 天 / 永久，預設 30 天）。
- 「重設網址」可隨時讓舊連結立即失效。
- 「關閉公開分享」一鍵停用並刪除公開資料。

### 公開瀏覽頁（PWA `/public/{token}`）

- 任何人持連結即可瀏覽，無須登入或安裝 Extension。
- 書名與作者即時搜尋，響應式排版（手機 / 平板 / 桌面）。
- 點擊書籍跳轉至讀墨購買介紹頁。
- 連結失效或網址錯誤會顯示友善錯誤頁面。

### 帳號彈性

- 已離開家庭的使用者也可繼續管理公開書櫃（auth refresh 不再強制要求 family membership）。

---

## v1.1.0（2026-04-28）

### 借閱功能

- 新增「借閱」分頁：在家庭書櫃對家人開放的書按下「申請借閱」即可送出申請。
- 書主收到申請後，按「同意借閱」會由 Extension 自動操作讀墨原生借出流程，僅需手動確認讀墨的最後一個系統對話框。
- 完整的借閱狀態機：申請中 / 已借出 / 已歸還 / 已拒絕 / 已取消，雙方皆可看到目前狀態。
- 新增 PENDING 申請的紅點通知（浮動按鈕與「借閱」分頁皆會顯示）。
- 「成員管理」可針對每位家人開關「允許他人向我借書」（`canLend`），並設定對應的讀墨成員名稱。

### PWA 行動版

- 同步顯示借閱清單與狀態，可申請借書、取消申請、標記已歸還。
- 受限於行動瀏覽器無法操作讀墨借出 UI，PWA 不提供「同意借閱」按鈕，會引導書主回到桌面 Extension 操作。

### 家庭管理

- 移除成員或自行離開家庭時，相關的「申請中」借閱會自動取消，避免懸而未決的請求。

### 安全與穩定性

- Worker 對 `PUT /api/user/:id/books` 與所有借閱 API 加上每位使用者每小時的 rate limit。
- Extension Service Worker 在 `chrome.action` 不存在時改為 no-op，避免在開發環境噴錯。
- 開發版 manifest 改寫為 idempotent，多次重新整理不會重覆改寫 icon。

---

## v1.0.0（2026-04-07）

首次公開發布。

### 家庭書櫃（核心功能）

- 在讀墨網頁中直接開啟「家庭書櫃」Dialog，不需離開閱讀頁面。
- 一鍵建立家庭，透過同步碼邀請家人加入。
- 瀏覽家庭成員開放的書籍，支援依成員篩選。
- 書名與作者即時搜尋。
- 書籍分類篩選（依讀墨主題分類）。
- 書櫃有新書時顯示紅點通知。

### 個人書櫃管理

- 所有書籍預設不開放，由你自行選擇要分享的書。
- 逐本切換開放/關閉，也可多選批次操作。
- 支援已封存書籍的同步與篩選。
- 變更後需手動儲存才會同步，不會意外洩露。

### 家庭管理

- 查看家庭成員列表（標示管理者）。
- 管理者可移除成員、轉移管理權。
- 離開家庭後，其他成員立即無法看到你的書。
- 個人開放設定不因離開家庭而消失，換家庭不用重新設定。

### 安全與隱私

- 安全存取控制：所有資料透過 HTTPS 傳輸，以認證 Token 控管存取權限。
- 不收集個人識別資訊，不需要額外帳號。
- 不使用 Cookie，不嵌入追蹤器。

### PWA 行動版

- 手機瀏覽器可透過 PWA 網頁使用家庭書櫃。
- 支援 QR Code 快速登入（從 Extension 掃碼）。
- 支援手動輸入同步碼 + Email 登入。
- 可選的登入驗證保護（PIN 碼、圖形鎖、OTP）。
- 個人書櫃開放設定（需先從 Extension 同步過書單）。
- 支援安裝至手機桌面。

### 自訂後端

- 支援自建 Cloudflare Worker 後端，完全掌控資料。
- 同步碼自動帶入 API 端點（`@host`），家人不需額外設定。
- Extension 與 PWA 均支援自訂 API 端點。

### 其他

- 可編輯顯示名稱（不影響讀墨帳號）。
- 登出時可選擇記住同步碼，方便重新登入。
- 重新安裝 Extension 時自動恢復家庭資料。
- 問題回報連結（GitHub / Plurk）。

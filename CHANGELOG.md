# 更新紀錄

所有使用者可見的變更都會記錄在這裡。

---

## v1.7.0（2026-08-28）

### 功能新增

- 家庭設定新增「解除移除限制」：管理者不小心移除成員後，按一下就能讓對方馬上重新加入，不必等 6 小時。這個動作只解除限制，不會自動把人加回家庭，對方仍要自己用同步碼加入；重複按也不會出錯
- 被鎖定或操作太頻繁時，畫面會直接顯示還要等幾秒並即時倒數，不必自己猜要等多久

### 問題修正

- **修正讀墨改版後擴充功能完全不能用的問題**：讀墨的書櫃換了網址（`next.readmoo.com`），舊版因此一本書都抓不到。現在新舊網址都支援，還沒換過去的帳號也能照常使用
- 修正忘記 PIN 後解不開的問題：在電腦上重設 PIN 或圖形驗證後，先前累積的失敗紀錄立刻失效，可以馬上在手機登入，不用等鎖定時間過完
- 修正用「隨機驗證碼」登入時一定會失敗的問題——驗證碼先前會在流程中途就被用掉
- 修正公開書櫃在「快到期的最後一分鐘」儲存書單或改設定會失敗的問題。現在系統會直接把即將消失的分享內容移除（最多提早 59 秒），不再回報錯誤。連結原本也是在這一分鐘內失效，實際感覺不到差別
- 修正「關閉公開分享」失敗卻顯示已關閉的問題：現在只有伺服器確認成功才會顯示已關閉，失敗會如實顯示錯誤，不會讓你以為連結撤銷了、其實還打得開
- 修正在一台裝置關閉分享後、另一台裝置可能把它重新打開的問題：先前另一台裝置在幾十秒內同步書單時，有機會把舊連結寫回去。現在關閉與重設一律立刻生效
- 修正 Firefox 上伺服器位址有時沒被記住、下次開啟又連回預設伺服器的問題
- 修正借閱狀態顯示異常的問題：收到預期外的狀態時改用安全的預設顯示，不會讓整個畫面出錯
- 修正手機版在網路不穩時會直接被登出的問題：暫時性的連線失敗現在會保留登入狀態並重試
- 修正儲存或同步後的「已儲存」等提示，偶爾被上一次的提示蓋掉或提早消失的問題
- 被家庭管理者移出時，擴充功能會在畫面上說明原因，不再無聲跳回一開始的引導畫面。家庭已解散、或人數已滿而無法重新加入時，同樣會說明原因
- 公開書櫃設定的錯誤提示全面中文化：操作太頻繁時顯示「嘗試次數過多，請於 N 秒後再試」；標題或到期日儲存失敗時會保留你的輸入、顯示「變更尚未儲存」並提供「重試儲存」按鈕，不再無聲丟掉你的修改。另外，把標題改回原值或重選同一個到期日時，不會再送出多餘的儲存請求

### 安全與隱私

- 「建立家庭」與「查詢所屬家庭」現在也需要通過驗證：有設定 PIN、圖形或驗證碼的人，別人無法再單憑你的 Email 幫你的帳號建立家庭並取得存取權，也查不到你的同步碼。沒設定驗證的人行為不變
  - **請更新擴充功能／PWA**：舊版不會送出驗證資訊，若你有設定 PIN／圖形／驗證碼，舊版會誤判成「尚未加入家庭」並在建立家庭時失敗。更新後即可正常使用；已加入家庭的日常操作不受影響
- 別人猜錯你的 PIN 不會再鎖住你：驗證失敗改成只鎖住猜錯的那個連線來源，不再記在你的帳號上
- 新增猜錯次數上限：同一個帳號每小時最多 10 次驗證失敗，避免有人一直換網路來源猜你的 PIN 或圖形。只有猜錯才計入，輸入正確不計，所以你自己怎麼登入都不會用掉額度
  - 這道上限不會把你自己擋在門外：就算有人把你帳號的額度用完，只要你輸入的是正確的 PIN、圖形或驗證碼，查詢、建立與加入家庭一律照常通過
- 「查詢所屬家庭」加上每分鐘 3 次的頻率限制，因為這個動作可以被拿來試密鑰。正常登入不受影響；短時間反覆重試才會被擋，並會顯示還要等幾秒
  - 它有自己獨立的額度，不會跟「建立／加入家庭」互搶。一次完整登入會用掉查詢 2 次、建立／加入 1 次，所以 PIN 打錯要重試、或家人接著在同一個網路下設定時，不會因為額度被前面用光而被擋下
- 公開書櫃、驗證設定與家庭設定的變更，各自加上每小時最多 30 次的上限，避免單一帳號短時間大量操作而影響到所有使用者。一般使用碰不到；超過時該動作會暫時失敗，稍候即可恢復
- 關閉分享或重設連結後，舊連結一定打不開了：先前在極少數情況下，舊連結可能還會繼續有效一段時間，現在不會。設有到期日的書櫃同樣適用
- 設有到期日的分享連結，過期後一律顯示「找不到或已過期」，不會再露出過期內容
- 家庭管理者更換伺服器不再無聲生效：改用（或改回官方）自訂伺服器時，其他成員會先看到確認提示，同時列出目前與即將切換的位址，同意後才會切換。這可避免管理者無聲把其他人的連線——連同登入憑證與完整書單——導向自己控制的伺服器
  - 選擇「暫不切換」會被記住，在位址再次變動前不會重複詢問
  - 畫面顯示的是實際會連線的完整位址（含 `http`／`https` 與路徑），所以區網的 `http` 位址不會被誤看成加密連線，偽裝成知名網域的位址也會現出原形
  - 若位址本身無效或不安全（例如網址裡夾帶帳號密碼，看起來連往 A 網站、實際連往 B 網站），畫面會改為顯示警告，不會把該位址原樣列出，按下加入也會直接中止，不會有任何資料送往那裡
  - 這則警告不會再於打字過程中反覆閃現：改成在你停下約 0.6 秒後才判定，貼上、離開欄位或按下加入時則立即判定。等待期間該處留白，不會沿用上一則提示
  - 加入失敗時不會把裝置留在那台伺服器上：只有確實加入成功才會保存位址。同步碼過期、輸入錯誤，或對方刻意回覆「找不到家庭」時，都會退回原本使用的伺服器
  - 手機掃到帶有自訂伺服器的 QR Code 時不會直接加入，會先顯示位址、等你按「確認並加入」。在你確認之前，手機不會向那台伺服器送出任何連線，也就不會洩漏你的網路位址。選「取消」會回到手動輸入，同步碼仍為你保留。官方伺服器的 QR Code 不受影響，掃了還是一鍵完成
  - 手機版現在與擴充功能採用同一套檢查與顯示規則。**若你的手機先前存過不符合這套檢查的伺服器位址**（例如網址中夾帶帳號密碼），為安全起見會被視為尚未登入，重新輸入一次同步碼即可恢復，之後不會再出現
  - 離開家庭或被移出時會一併還原伺服器設定，回到官方伺服器，避免已經沒有家庭的裝置還指向前一個家庭的伺服器
- 登入憑證過期後跳出的「需要驗證」視窗，現在也會顯示即將連線的伺服器位址。先前只有加入家庭時會顯示，重新驗證時看不到自己的 PIN 會送去哪裡
- 管理者移除成員現在會真的生效：被移除的人 6 小時內無法重新加入同一個家庭，包含背景自動重連、手動輸入同步碼與掃 QR Code。6 小時過後持同步碼重新加入仍然可行，這也是誤移除的補救方式。自願離開家庭不受影響，隨時可以再加入
  - 被移除的人會即時知道並登出：擴充功能回到引導畫面，手機顯示「你已被家庭管理者移出，已為你登出」
  - **請更新擴充功能／PWA**：舊版不認得這個回應，會在背景一直重試又不說原因，6 小時後甚至可能自動重新加入。更新後會正確登出並顯示原因
- 修補借閱請求可被植入追蹤圖片的漏洞：建立借閱請求時，書封網址必須是 Readmoo 的網址（`readmoo.com`／`readmoo.tw` 及其子網域），其他一律拒絕，避免有人用改造過的程式藉自訂書封取得其他家人的 IP 與開啟時間。擴充功能與手機版在顯示前也會再過濾一次，先前已存在的異常書封會顯示為空白書封
- 加強對「伺服器回傳怪資料」的防護：如果你的家庭改用自訂伺服器，而那台伺服器回傳了格式不對或帶有惡意內容的資料（書名、成員名稱、借閱紀錄、驗證方式等），擴充功能與手機版現在會直接忽略或改用安全的預設值顯示，不會讓畫面錯亂或誤導你。使用官方伺服器的人不受影響，日常使用也感覺不到差別
- 家庭自訂伺服器不能再指向內網位址：除了原本就擋掉的私有 IPv4，現在也拒絕所有 IPv6 字面位址與 127.x.x.x 迴環位址（本機開發用的 127.0.0.1 不受影響）。這只擋常見的內網字面位址，指向內網的網域名稱不在辨識範圍內

---

## v1.6.1（2026-07-25）

### 介面調整

- 對話框互動元件補上一致的 hover / focus 樣式

### 安全與穩定性

- 擴充功能更新或重新載入後，舊分頁的對話框不再出現「Extension context invalidated」錯誤，改以預設值安全降級

---

## v1.6.0（2026-07-09）

### 功能新增

- 家庭書櫃新增「我的最愛」：可標記喜歡的書並切換「只看最愛」，僅影響自己的檢視，家人不會知道你標記了哪些書
- 書櫃排序新增降冪（Z→A）選項，書名與作者皆可反向排序
- 篩選 / 排序選單改為「圖示 + 文字」樣式，分頁加上圖示，並優化手機版版面與標題呈現
- 對話框支援手機版響應式版面（含 Firefox 行動版）
- 書籍排序下拉選單支援鍵盤操作（方向鍵 / Enter / Esc）

### 問題修正

- 修正桌面版對話框在主畫面的高度異常
- 修正 Firefox 上 familyId 有時無法保存的問題
- 修正 Firefox 擴充功能的資料蒐集權限宣告（附加元件商店上架合規）

### 安全與穩定性

- 強化家庭「重新加入 / 復原」驗證：有設定 PIN / 圖形驗證的使用者，在新裝置重新加入或復原時會被要求輸入驗證，避免他人冒用帳號；同時強化後端輸入驗證
- 修正公開書櫃設定：在標題輸入後隨即關閉視窗時，不再送出未完成的變更
- 更新後端相依套件（hono），修補安全告警

---

## v1.5.0（2026-06-18）

### 功能新增

- 家庭書櫃新增「隱藏書籍」：可把不想看到的書隱藏，僅影響自己的檢視（家人不受影響），可隨時取消隱藏，並能切換檢視已隱藏的書
- 推出 Firefox 版擴充功能，支援 Firefox 桌面版與 Firefox for Android™：可從 Firefox 附加元件商店安裝，或下載簽章版 `.xpi` 自行安裝（含自動更新）

---

## v1.4.0（2026-06-11）

### 功能新增

- 家庭書櫃支援 Grid / Row 兩種顯示模式，偏好自動記憶
- Extension 浮動圖示可自選大小（小 / 中 / 大）
- 個人書櫃與家庭書櫃新增書籍排序選項（書名、作者、預設順序）
- 書櫃自動同步頻率可自訂，開啟 Dialog 不再自動捲動書庫
- PWA 借閱流程新增手動借出引導，提示使用者自行從讀墨完成借書操作
- 邀請連結新增歡迎訊息與加入步驟說明，方便新成員快速上手

### 效能改善

- 書單同步改為差異上傳（PATCH API），大幅減少網路流量

### 問題修正

- 修正 PWA 表單欄位與按鈕高度不一致的問題

---

## v1.3.0（2026-05-29）

### 功能新增

- 借閱流程簡化：移除手動輸入讀墨成員名稱的步驟，系統自動從家庭成員清單取得
- 官網 footer 新增「問題回報」入口

### 效能改善

- 大量藏書效能優化：200 本以上書籍的載入、篩選與搜尋速度大幅提升，虛擬捲動與 memo 減少不必要渲染
- 儲存路徑效能優化：儲存分享設定時僅重繪異動項目，避免整頁重新渲染

### 介面調整

- 家庭書櫃書籍封面放大為 120×180，更容易辨識書籍

### 問題修正

- 修正顯示名稱在家庭紀錄、個人紀錄與 Extension 快取之間未同步的問題
- 修正 PWA 多項 UI 問題（底部列對齊、捲動行為）與 CORS preflight 設計

### 開發者體驗

- 新增 dev-only 互動式 API 文件（Swagger UI）：`/api/_docs` 可直接測試所有 API，僅 dev 環境開啟

---

## v1.2.1（2026-05-06）

### 新功能

- 「問題回報」新增 Google 表單入口：點擊圖示直接開啟回報表單，並自動帶入目前 Extension 版本，使用者不需手動填寫

### 問題修正

- QR Code 連結改為按需產生：開啟 Dialog 不再立即向後端索取 token，僅在使用者點擊「產生連結」時才發起 API 呼叫，避免不必要的請求
- 修正 Dialog 載入畫面 footer 底部出現的留白
- 載入畫面新增 spinner 動畫，提供清楚的載入中提示
- 修正 PWA 個人書櫃批次操作工具列無法停留在視窗底部的問題；工具列現在會固定在底部 Tab Bar 正上方並支援 iOS 安全區

---

## v1.2.0（2026-05-03）

### 公開書櫃分享

- 個人書櫃新增「公開分享」按鈕：可產生獨立網址，分享給家庭外的朋友瀏覽
- 公開書櫃內容 = 你已開放的書（與家庭分享同一組），更新書單時公開頁面自動同步
- 自訂書櫃標題與過期時間（7 / 30 / 60 / 90 天 / 永久，預設 30 天）
- 「重設網址」可隨時讓舊連結立即失效
- 「關閉公開分享」一鍵停用並刪除公開資料

### 公開瀏覽頁（PWA `/public/{token}`）

- 任何人持連結即可瀏覽，無須登入或安裝 Extension
- 書名與作者即時搜尋，響應式排版（手機 / 平板 / 桌面）
- 點擊書籍跳轉至讀墨購買介紹頁
- 連結失效或網址錯誤會顯示友善錯誤頁面

### 帳號彈性

- 已離開家庭的使用者也可繼續管理公開書櫃（auth refresh 不再強制要求 family membership）

---

## v1.1.0（2026-04-28）

### 借閱功能

- 新增「借閱」分頁：在家庭書櫃對家人開放的書按下「申請借閱」即可送出申請
- 書主收到申請後，按「同意借閱」會由 Extension 自動操作讀墨原生借出流程，僅需手動確認讀墨的最後一個系統對話框
- 完整的借閱狀態機：申請中 / 已借出 / 已歸還 / 已拒絕 / 已取消，雙方皆可看到目前狀態
- 新增 PENDING 申請的紅點通知（浮動按鈕與「借閱」分頁皆會顯示）
- 「成員管理」可針對每位家人開關「允許他人向我借書」（`canLend`），並設定對應的讀墨成員名稱

### PWA 行動版

- 同步顯示借閱清單與狀態，可申請借書、取消申請、標記已歸還
- 受限於行動瀏覽器無法操作讀墨借出 UI，PWA 不提供「同意借閱」按鈕，會引導書主回到桌面 Extension 操作

### 家庭管理

- 移除成員或自行離開家庭時，相關的「申請中」借閱會自動取消，避免懸而未決的請求

### 安全與穩定性

- Worker 對 `PUT /api/user/:id/books` 與所有借閱 API 加上每位使用者每小時的 rate limit
- Extension Service Worker 在 `chrome.action` 不存在時改為 no-op，避免在開發環境噴錯
- 開發版 manifest 改寫為 idempotent，多次重新整理不會重覆改寫 icon

---

## v1.0.0（2026-04-07）

首次公開發布。

### 家庭書櫃（核心功能）

- 在讀墨網頁中直接開啟「家庭書櫃」Dialog，不需離開閱讀頁面
- 一鍵建立家庭，透過同步碼邀請家人加入
- 瀏覽家庭成員開放的書籍，支援依成員篩選
- 書名與作者即時搜尋
- 書籍分類篩選（依讀墨主題分類）
- 書櫃有新書時顯示紅點通知

### 個人書櫃管理

- 所有書籍預設不開放，由你自行選擇要分享的書
- 逐本切換開放/關閉，也可多選批次操作
- 支援已封存書籍的同步與篩選
- 變更後需手動儲存才會同步，不會意外洩露

### 家庭管理

- 查看家庭成員列表（標示管理者）
- 管理者可移除成員、轉移管理權
- 離開家庭後，其他成員立即無法看到你的書
- 個人開放設定不因離開家庭而消失，換家庭不用重新設定

### 安全與隱私

- 安全存取控制：所有資料透過 HTTPS 傳輸，以認證 Token 控管存取權限
- 不收集個人識別資訊，不需要額外帳號
- 不使用 Cookie，不嵌入追蹤器

### PWA 行動版

- 手機瀏覽器可透過 PWA 網頁使用家庭書櫃
- 支援 QR Code 快速登入（從 Extension 掃碼）
- 支援手動輸入同步碼 + Email 登入
- 可選的登入驗證保護（PIN 碼、圖形鎖、OTP）
- 個人書櫃開放設定（需先從 Extension 同步過書單）
- 支援安裝至手機桌面

### 自訂後端

- 支援自建 Cloudflare Worker 後端，完全掌控資料
- 同步碼自動帶入 API 端點（`@host`），家人不需額外設定
- Extension 與 PWA 均支援自訂 API 端點

### 其他

- 可編輯顯示名稱（不影響讀墨帳號）
- 登出時可選擇記住同步碼，方便重新登入
- 重新安裝 Extension 時自動恢復家庭資料
- 問題回報連結（GitHub / Plurk）

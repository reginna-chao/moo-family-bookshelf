# 更新紀錄

所有使用者可見的變更都會記錄在這裡。

---

## v1.6.1（2026-07-25）

### 介面調整

- 對話框互動元件補上一致的 hover / focus 樣式

### 安全與穩定性

- 擴充功能更新或重新載入後，舊分頁的對話框不再出現「Extension context invalidated」錯誤，改以預設值安全降級
- 修正 PWA 驗證鎖定機制：驗證失敗次數改以「連線來源 + 目標帳號」計算並會自動過期。他人重複輸入錯誤的 PIN／圖形驗證只會鎖住他自己的連線來源，不再記在你的帳號上，也無法再用這個方式鎖住你的帳號
- 修正忘記 PIN 時的解鎖流程：在桌面版重設 PIN／圖形驗證後，先前累積的驗證失敗紀錄即失效，可立即在 PWA 登入，不必等鎖定時間結束
- 修正「建立家庭」與「查詢所屬家庭」缺少身分驗證的問題：有設定 PIN／圖形／驗證碼的使用者，這兩個動作現在也需要通過驗證，他人無法再單憑你的 Email 為你的帳號建立家庭並取得存取權，或查出你的同步碼。未設定驗證者行為不變
  - **請更新擴充功能／PWA**：舊版不會送出驗證資訊，若你有設定 PIN／圖形／驗證碼，舊版會誤判成「尚未加入家庭」並在建立家庭時失敗。更新後即可正常使用；已加入家庭的日常操作不受影響，未設定驗證者也不受影響
- 補上驗證的整體嘗試上限：同一個帳號每小時最多 10 次「驗證失敗」，避免有人不斷更換網路來源猜測你的 PIN／圖形。只有猜錯才計入，輸入正確或只是探詢都不計，因此你自己正常登入多少次都不會用掉額度。超過時會回覆「請稍後再試」並附上等待秒數
  - 這道上限不會把你自己擋在門外：即使有人用錯誤密鑰把你帳號的額度打滿，只要你輸入的是正確的 PIN／圖形／驗證碼，查詢、建立與加入家庭一律照常通過；被擋下的只有後續的錯誤猜測
- 收緊「查詢所屬家庭」的請求頻率：此動作已可用來測試密鑰，因此比照「建立／加入家庭」納入最嚴格的速率限制（同一網路來源每分鐘 3 次）。正常登入流程不受影響；短時間內反覆重試才可能收到「請稍後再試」，並附上等待秒數
  - 「查詢所屬家庭」有自己獨立的額度，不與「建立／加入家庭」互相排擠：一次完整的登入流程會用掉查詢額度 2 次、建立／加入額度 1 次，因此 PIN 打錯要重試、或家人在同一個網路下接著設定時，不會因為額度被前面的步驟用光而被擋下
- 修正使用「隨機驗證碼」登入時，驗證碼會在流程中途被提前用掉、導致必定失敗的問題
- 修正公開書櫃在「到期前最後一分鐘」儲存書單或更新書櫃設定會失敗的問題：此時系統改為直接移除即將消失的公開快照（最多提前 59 秒），不再回報錯誤。書櫃連結原本也是在這一分鐘內失效，實際觀感不變
- 收緊家庭自訂端點的位址檢查：除了原有的私有 IPv4 位址外，現在也一併拒絕所有 IPv6 字面位址與 127.x.x.x 迴環位址（本機開發用的 127.0.0.1 不受影響）。此檢查僅防止管理者把家人的連線導向常見的內網字面位址；指向內網的網域名稱不在辨識範圍內
- 修正家庭 API 的兩個更新動作（顯示名稱、自訂端點）收到格式錯誤的請求內容時回覆 500 的問題，現在會正確回覆 400
- 公開書櫃的寫入動作（建立、更新設定、重設連結、刪除）新增每帳號每小時 30 次的合計上限，防止單一帳號快速消耗後端每日的儲存額度而影響所有使用者。一般使用不會碰到；超過時該動作會暫時失敗，稍候即可恢復
- 補強公開書櫃連結的到期檢查：設有到期日的書櫃即使快照因罕見異常未被如期清除，逾期讀取一律回覆「找不到或已過期」，不再露出逾期內容
- 驗證設定的寫入動作（設定驗證方式、產生驗證碼、標記已提醒、產生 QR Code）新增每帳號每小時 30 次的合計上限，避免單一帳號快速消耗後端每日的儲存額度而影響所有使用者。一般使用不會碰到；超過時該動作會暫時失敗，稍候即可恢復。此額度與「驗證猜錯次數上限」互相獨立，他人猜錯不會佔用你的設定額度
- 修正「關閉公開分享」失敗時畫面誤顯示已關閉的問題：關閉動作被後端拒絕（例如操作過於頻繁）時，現在會如實顯示錯誤並保留書櫃狀態，只有伺服器確認成功才會顯示已關閉，連結不會在你以為已撤銷的情況下繼續有效
- 公開書櫃設定的錯誤提示全面中文化：操作過於頻繁時顯示「嘗試次數過多，請於 N 秒後再試」；標題或到期日儲存失敗時會保留你的輸入、顯示「變更尚未儲存」並提供「重試儲存」按鈕，不再無聲丟失變更。另外，把標題改回原值或重選相同到期日時不再送出多餘的儲存請求
- 補強「永久」公開書櫃的撤銷保證：重設連結或刪除資料的內部清理步驟若因罕見異常留下舊的公開快照，現在讀取時會比對你帳號中該書櫃的現行連結與設定，不符一律回覆「找不到」——舊連結不再無限期可讀。設有到期日的書櫃同樣納入這道比對（因此每次有效讀取會多查一次你的公開書櫃清單）；其快照若因同類罕見異常殘留、或被其他裝置寫回，也是立即回覆「找不到」，不會再服務到原設定的到期日
- 修正公開書櫃的撤銷可能被另一台裝置「還原」的問題：公開書櫃清單改以獨立資料存放，書單同步不再會寫入它。先前若你在某台裝置關閉分享或重設連結，另一台裝置在數十秒內自動同步書單，有機會把舊連結寫回並重新公開你的書單；現在關閉與重設一律立即生效，且撤銷保證已擴及所有公開書櫃（不再只限「永久」書櫃）。此調整無須手動遷移，你下次調整公開書櫃設定時自動完成
- 家庭自訂伺服器變更不再靜默套用：建立家庭的人改用（或改回官方）自訂伺服器後，其他成員會先看到確認提示，同時列出目前與即將切換的位址，同意後才會切換；選擇「暫不切換」會被記住，在位址再次變動前不會重複詢問。這可避免家中管理者無聲把其他人的連線（連同登入憑證與完整書單）導向自己控制的伺服器。同意後的切換也會立即寫入本機設定，Firefox 上不會再發生「已同意卻沒生效」的情況；下次開啟對話框時，也會直接從本機設定讀回已保存的伺服器位址，不會再無聲連回預設伺服器
  - 若管理者設定的位址本身無效或不安全，確認提示不會把該位址原樣列出（列出等於替偽造位址背書），改為在同一處顯示警告並提醒向管理者確認；此時無論如何都不會切換
  - 離開家庭時會一併還原伺服器設定：自訂伺服器位址與先前記住的「暫不切換」選擇都會清除，回到官方伺服器。這可避免已經沒有家庭的裝置仍指向前一個家庭的伺服器，下次建立或加入家庭時也會重新詢問
- 貼上帶有自訂伺服器的同步碼（結尾帶有 @ 與伺服器位址）時，加入畫面會先顯示即將連線的伺服器位址；加入後該位址也會確實保存，修正 Firefox 上偶爾沒保存、下次開啟又連回預設伺服器的問題
  - 顯示的是實際會連線的完整位址（含 http／https 與路徑），因此區網的 http 位址不會被誤看成加密連線，偽裝成知名網域的位址也會現出原形
  - 若同步碼裡的伺服器位址無效或不安全（例如網址內嵌帳號密碼，看起來連往 A 網站、實際連往 B 網站），畫面會改為顯示警告而不是「將連線至自訂伺服器」那句提示，按下加入也會直接中止，不會有任何資料送往該位址
  - 這則警告不再於逐字輸入的過程中反覆閃現：改為在你停下約 0.6 秒後判定，貼上、離開欄位或按下加入時則立即判定。等待判定期間該處一律留白，不會沿用上一則提示——否則在你剛把另一個位址接到後面的當下，畫面上那句舊的「將連線至自訂伺服器」等於替新位址背書。正常位址的提示本身不受影響，仍即時顯示；從邀請連結或 QR Code 帶進來的同步碼也是一進畫面就顯示，不必等待
  - 加入失敗時不會把裝置留在同步碼指定的伺服器上：該位址只有在確實加入成功後才會保存。同步碼過期、輸入錯誤，或該伺服器刻意回覆「找不到家庭」時，裝置都會退回原本使用的伺服器，中途取消也一樣。以往這類失敗會讓裝置就此指向該伺服器，接著按下「建立家庭」便會把登入憑證與完整書單送往那裡
  - PWA 現在與擴充功能採用同一套位址檢查與顯示規則：從同步碼、邀請連結或 QR Code 帶進來的伺服器位址一樣會先檢查、先顯示，驗不過就不加入、不保存。若你的手機先前已存下不符合這套檢查的伺服器位址（例如網址中夾帶帳號密碼），為安全起見會視為尚未登入，只要重新輸入一次同步碼即可恢復，之後不會再出現
  - 手機掃描帶有自訂伺服器位址的 QR Code 時不再直接加入：畫面會先顯示即將連線的伺服器位址，等你按下「確認並加入」才會繼續。在你確認之前，手機不會向那台伺服器送出任何連線（連查詢驗證方式的探詢都不會送出），因此也不會洩漏你的網路位址。選擇「取消」會回到手動輸入畫面，同步碼仍為你保留，可以改用其他同步碼或直接離開。使用官方預設伺服器的 QR Code 不受影響，掃描後仍是一鍵完成，操作步驟完全不變，不需要多按任何一下
- 登入憑證過期後跳出的「需要驗證」視窗現在也會顯示即將連線的伺服器位址。先前只有加入家庭的流程會顯示，重新驗證時看不到自己的 PIN／圖形將送往哪台伺服器；現在只要目前使用的不是官方伺服器，輸入前就會先看到完整位址
  - 一併修正提示措辭：畫面上看不到同步碼時（驗證挑戰、重新驗證），提示不再稱「此同步碼將連線至⋯」，改為「將連線至⋯」。重新驗證發生在加入家庭很久之後，過程中並沒有同步碼參與

---

## v1.6.0（2026-07-09）

### 功能新增

- 家庭書櫃新增「我的最愛」：可標記喜歡的書並切換「只看最愛」，僅影響自己的檢視，家人不會知道你標記了哪些書
- 書櫃排序新增降冪（Z→A）選項，書名與作者皆可反向排序
- 篩選 / 排序選單改為「圖示 + 文字」樣式，分頁加上圖示，並優化手機版版面與標題呈現
- 對話框支援手機版響應式版面（含 Firefox 行動版）
- 書籍排序下拉選單支援鍵盤操作（方向鍵 / Enter / Esc）

### 問題修正

- 修正桌面版對話框在主畫面的高度異常
- 修正 Firefox 上 familyId 有時無法保存的問題
- 修正 Firefox 擴充功能的資料蒐集權限宣告（附加元件商店上架合規）

### 安全與穩定性

- 強化家庭「重新加入 / 復原」驗證：有設定 PIN / 圖形驗證的使用者，在新裝置重新加入或復原時會被要求輸入驗證，避免他人冒用帳號；同時強化後端輸入驗證
- 修正公開書櫃設定：在標題輸入後隨即關閉視窗時，不再送出未完成的變更
- 更新後端相依套件（hono），修補安全告警

---

## v1.5.0（2026-06-18）

### 功能新增

- 家庭書櫃新增「隱藏書籍」：可把不想看到的書隱藏，僅影響自己的檢視（家人不受影響），可隨時取消隱藏，並能切換檢視已隱藏的書
- 推出 Firefox 版擴充功能，支援 Firefox 桌面版與 Firefox for Android™：可從 Firefox 附加元件商店安裝，或下載簽章版 `.xpi` 自行安裝（含自動更新）

---

## v1.4.0（2026-06-11）

### 功能新增

- 家庭書櫃支援 Grid / Row 兩種顯示模式，偏好自動記憶
- Extension 浮動圖示可自選大小（小 / 中 / 大）
- 個人書櫃與家庭書櫃新增書籍排序選項（書名、作者、預設順序）
- 書櫃自動同步頻率可自訂，開啟 Dialog 不再自動捲動書庫
- PWA 借閱流程新增手動借出引導，提示使用者自行從讀墨完成借書操作
- 邀請連結新增歡迎訊息與加入步驟說明，方便新成員快速上手

### 效能改善

- 書單同步改為差異上傳（PATCH API），大幅減少網路流量

### 問題修正

- 修正 PWA 表單欄位與按鈕高度不一致的問題

---

## v1.3.0（2026-05-29）

### 功能新增

- 借閱流程簡化：移除手動輸入讀墨成員名稱的步驟，系統自動從家庭成員清單取得
- 官網 footer 新增「問題回報」入口

### 效能改善

- 大量藏書效能優化：200 本以上書籍的載入、篩選與搜尋速度大幅提升，虛擬捲動與 memo 減少不必要渲染
- 儲存路徑效能優化：儲存分享設定時僅重繪異動項目，避免整頁重新渲染

### 介面調整

- 家庭書櫃書籍封面放大為 120×180，更容易辨識書籍

### 問題修正

- 修正顯示名稱在家庭紀錄、個人紀錄與 Extension 快取之間未同步的問題
- 修正 PWA 多項 UI 問題（底部列對齊、捲動行為）與 CORS preflight 設計

### 開發者體驗

- 新增 dev-only 互動式 API 文件（Swagger UI）：`/api/_docs` 可直接測試所有 API，僅 dev 環境開啟

---

## v1.2.1（2026-05-06）

### 新功能

- 「問題回報」新增 Google 表單入口：點擊圖示直接開啟回報表單，並自動帶入目前 Extension 版本，使用者不需手動填寫

### 問題修正

- QR Code 連結改為按需產生：開啟 Dialog 不再立即向後端索取 token，僅在使用者點擊「產生連結」時才發起 API 呼叫，避免不必要的請求
- 修正 Dialog 載入畫面 footer 底部出現的留白
- 載入畫面新增 spinner 動畫，提供清楚的載入中提示
- 修正 PWA 個人書櫃批次操作工具列無法停留在視窗底部的問題；工具列現在會固定在底部 Tab Bar 正上方並支援 iOS 安全區

---

## v1.2.0（2026-05-03）

### 公開書櫃分享

- 個人書櫃新增「公開分享」按鈕：可產生獨立網址，分享給家庭外的朋友瀏覽
- 公開書櫃內容 = 你已開放的書（與家庭分享同一組），更新書單時公開頁面自動同步
- 自訂書櫃標題與過期時間（7 / 30 / 60 / 90 天 / 永久，預設 30 天）
- 「重設網址」可隨時讓舊連結立即失效
- 「關閉公開分享」一鍵停用並刪除公開資料

### 公開瀏覽頁（PWA `/public/{token}`）

- 任何人持連結即可瀏覽，無須登入或安裝 Extension
- 書名與作者即時搜尋，響應式排版（手機 / 平板 / 桌面）
- 點擊書籍跳轉至讀墨購買介紹頁
- 連結失效或網址錯誤會顯示友善錯誤頁面

### 帳號彈性

- 已離開家庭的使用者也可繼續管理公開書櫃（auth refresh 不再強制要求 family membership）

---

## v1.1.0（2026-04-28）

### 借閱功能

- 新增「借閱」分頁：在家庭書櫃對家人開放的書按下「申請借閱」即可送出申請
- 書主收到申請後，按「同意借閱」會由 Extension 自動操作讀墨原生借出流程，僅需手動確認讀墨的最後一個系統對話框
- 完整的借閱狀態機：申請中 / 已借出 / 已歸還 / 已拒絕 / 已取消，雙方皆可看到目前狀態
- 新增 PENDING 申請的紅點通知（浮動按鈕與「借閱」分頁皆會顯示）
- 「成員管理」可針對每位家人開關「允許他人向我借書」（`canLend`），並設定對應的讀墨成員名稱

### PWA 行動版

- 同步顯示借閱清單與狀態，可申請借書、取消申請、標記已歸還
- 受限於行動瀏覽器無法操作讀墨借出 UI，PWA 不提供「同意借閱」按鈕，會引導書主回到桌面 Extension 操作

### 家庭管理

- 移除成員或自行離開家庭時，相關的「申請中」借閱會自動取消，避免懸而未決的請求

### 安全與穩定性

- Worker 對 `PUT /api/user/:id/books` 與所有借閱 API 加上每位使用者每小時的 rate limit
- Extension Service Worker 在 `chrome.action` 不存在時改為 no-op，避免在開發環境噴錯
- 開發版 manifest 改寫為 idempotent，多次重新整理不會重覆改寫 icon

---

## v1.0.0（2026-04-07）

首次公開發布。

### 家庭書櫃（核心功能）

- 在讀墨網頁中直接開啟「家庭書櫃」Dialog，不需離開閱讀頁面
- 一鍵建立家庭，透過同步碼邀請家人加入
- 瀏覽家庭成員開放的書籍，支援依成員篩選
- 書名與作者即時搜尋
- 書籍分類篩選（依讀墨主題分類）
- 書櫃有新書時顯示紅點通知

### 個人書櫃管理

- 所有書籍預設不開放，由你自行選擇要分享的書
- 逐本切換開放/關閉，也可多選批次操作
- 支援已封存書籍的同步與篩選
- 變更後需手動儲存才會同步，不會意外洩露

### 家庭管理

- 查看家庭成員列表（標示管理者）
- 管理者可移除成員、轉移管理權
- 離開家庭後，其他成員立即無法看到你的書
- 個人開放設定不因離開家庭而消失，換家庭不用重新設定

### 安全與隱私

- 安全存取控制：所有資料透過 HTTPS 傳輸，以認證 Token 控管存取權限
- 不收集個人識別資訊，不需要額外帳號
- 不使用 Cookie，不嵌入追蹤器

### PWA 行動版

- 手機瀏覽器可透過 PWA 網頁使用家庭書櫃
- 支援 QR Code 快速登入（從 Extension 掃碼）
- 支援手動輸入同步碼 + Email 登入
- 可選的登入驗證保護（PIN 碼、圖形鎖、OTP）
- 個人書櫃開放設定（需先從 Extension 同步過書單）
- 支援安裝至手機桌面

### 自訂後端

- 支援自建 Cloudflare Worker 後端，完全掌控資料
- 同步碼自動帶入 API 端點（`@host`），家人不需額外設定
- Extension 與 PWA 均支援自訂 API 端點

### 其他

- 可編輯顯示名稱（不影響讀墨帳號）
- 登出時可選擇記住同步碼，方便重新登入
- 重新安裝 Extension 時自動恢復家庭資料
- 問題回報連結（GitHub / Plurk）

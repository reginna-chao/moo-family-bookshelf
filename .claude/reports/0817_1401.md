# Retro 0817_1401 — fix/public-sharing-lost-update(publicSharing lost-update 設計缺陷修復)

## 1. Run 概要

| 項目                             | 值                                                                                                                                                 |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| Intent route                     | CODE(backend 單一 scope)                                                                                                                           |
| Agent 派遣                       | 11 執行(coder 4、tester 4、reviewer 2、security-auditor 1)+ 1 次 tester 派遣被使用者中斷後原樣重派                                                 |
| Fix Cycle rounds                 | 2(R1:0 CRITICAL / 6 SUGGESTION;R2:0 CRITICAL / 2 SUGGESTION + 1 順手項)+ 1 輪 security WARNING 修復                                                |
| Gate rounds                      | 需求確認 1(AskUserQuestion 三項設計決策一次過)/ verify-before-test 3 個往返(指示不夠具體 → 代跑 → 使用者自測)/ SUGGESTION 決策 2 + security 決策 1 |
| Diff stat(vs origin/main,staged) | 13 files, +1972 / −222                                                                                                                             |
| Wall-clock 瓶頸                  | 首輪 tester 派遣 1016 秒                                                                                                                           |
| 測試                             | 1004(基線)→ 1076 全綠;typecheck / lint 全綠;security scan PASS WITH WARNINGS → WARNING 已修                                                        |

## 2. 卡點與摩擦

- **U1**(verbatim)「這個可以說明更清楚一點嗎？例如應該要怎麼帶入要去哪裡看數值，dev 本地的資料應該不能看 cloudflare 上的資料」— verify gate 的人工驗證指示假設使用者熟 wrangler/PowerShell，缺「數值在哪、用什麼指令看」。
- **U2**(verbatim)「我進入這個畫面後完全不知道該怎麼辦」（附 wrangler dev 截圖）— 且 dev server 起在**主 checkout**（舊程式碼）而非 worktree；最終由 orchestrator 在 8788 代跑全部三段驗證才通過 gate。
- **U3** AskUserQuestion 以自由文字回「我還在測試」— gate 保持開啟、補交測試環境 cheat sheet，流程無損。
- **U4** tester 派遣一度被中斷，「繼續派 tester」後原樣重派，零重工。
- **G1** `wrangler kv key put --local` 因 wrangler.toml 同時有 `id`/`preview_id` 而要求 `--preview false`，驗證 B 注入失敗重跑一輪（`get`/`list` 不受影響）。
- **G2** 兩個 `wrangler dev` 實例共用 dev-registry 目錄，一方關閉時把另一方打掛（ENOENT utime，wrangler 3.x 已知競態）— 背景測試 server 過夜死亡。
- **G3** 基底分支 `fix/permanent-shelf-orphan-liveness` 於 run 中途被 squash 併入 main（5529d49 → 2809845），收尾時以 stash → reset --hard origin/main → pop 重掛，CHANGELOG 一處衝突手解（upstream 舊句 vs 本支修訂句）。

## 3. 改進提案

### Flow-structural（L#）

- **L0817_1401-1** `target=.claude/skills/develop/references/code-cycle.md | sketch=Phase 3 verify gate：backend scope 預設由 orchestrator 代跑驗證並呈現觀察結果，copy-paste 指令僅作備選 | status=proposed`（證據：U1+U2 兩輪摩擦，代跑後一次通過）
- **L0817_1401-2** `target=.claude/skills/develop/references/code-cycle.md | sketch=Phase 0 第 6 點只管 agent prompt；補「給使用者的手動驗證指示也必須明示 worktree 路徑並警告主 checkout 是不同程式碼」 | status=proposed`（證據：U2 的 8787 主 checkout server）
- **L0817_1401-3** `target=.claude/rules/backend.md（Commands 節） | sketch=補兩行本機事實：kv put --local 需 --preview false；多個 wrangler dev 共用 dev-registry 會互相打掛 | status=proposed`（證據：G1、G2）
- **L0817_1401-4** `target=.claude/rules/test.md（Mock Policy） | sketch=補一行 worker/tests/helpers/kvOps.ts 的 watchKvOps/writeTrail() 寫入順序 tripwire 的存在與用途 | status=proposed`（tester 主動建議，避免日後重新發明）

### 執行槓桿（E#）

- **E1** 大 diff 首輪 tester 派遣時附上既有 suite helper 清單（seed/assert/watchKvOps 等），砍探索讀取 — tester 為本 run 最大 token 消耗類別。
- **E2** 交付本機測試環境給使用者時一次性輸出 cheat sheet（base URL / uid / token / 活・死 token 對照 + 限流表）— 本 run 有效，省往返。
- **E3** run 跨多日或基底為未合併分支時，commit gate 前固定 `git fetch --prune` 檢查基底是否已被合併 — 本 run 靠 diff 報錯才發現。

## 4. Skill / Agent 增減建議

無新增／移除提案。tester 的 prompt 慣例改進由 E1 承載；單一 run 訊號不足以動 agent 結構。

## 5. 三軸 KPI

- speed: `bottleneck_seconds: 1016`、`serialized_legs: 0`
- precision: `requirement_rounds: 1`、`critical_count: 0`、`suggestion_adopted/proposed: 9/9`（另 security WARNING 1/1 修復）、`rework_rt: 1`
- context: `dispatches: 11`、`self_reads: 8`、`top_consumer: tester 派遣（約 60 萬 subagent tokens）`、`lever: E1（helper 清單前置）`
- meta: `patch_proposed: 4`

# Retro — 0812_0822（fix/public-shelf-min-ttl）

## 1. Run 概要

| 項目             | 值                                                                                                                        |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------- |
| Intent route     | CODE（backend bug fix，fast-path：root cause 由使用者以 file:line 釘死）                                                  |
| Agent 派遣       | 5 次嘗試 / 4 次有效（coder 1×中斷但編輯已完整落地、tester 1、reviewer 2×其中 1 次啟動前被使用者中斷、security-auditor 1） |
| Fix Cycle rounds | 1（無 CRITICAL；SUGGESTION 3 提 3 採）                                                                                    |
| Gate rounds      | 需求確認 0（fast-path 併入原始需求）／verify-before-test 0（使用者已預先釘死行為）／SUGGESTION 決策 1                     |
| Diff stat        | 4 files, +23/−5（另加本報告）                                                                                             |
| Wall-clock 瓶頸  | reviewer 996s（30 tool uses）；security-auditor 415s；coder 476s（中斷）；tester 186s                                     |

## 2. 卡點與摩擦

- **U1**：首次 reviewer 派遣被使用者中斷，隨後指示「重新派遣 reviewer 繼續」。原因為前一晚 session limit 中斷後的接續，非內容修正。
- **U2**：「Try again」— 收尾報告該輪的 AskUserQuestion（commit gate + retro offer）未送達使用者，Stop Block 出現但問題本體缺席，需使用者提醒後補發。
- **G1**：coder 派遣於 ~476s 時因 API session limit 死亡；恢復時發現其編輯已完整且與處方逐字一致，依「處方例外」由 orchestrator 驗 diff 取代重派（SKILL.md 的 dead-agent 規則字面上要求一律重派，與處方例外精神有張力，見 L0812_0822-2）。
- **G2**：**Run 中上游漂移** — 父分支 `test/public-shelf-service-unit` 在本 run 進行期間以 #96 squash 合入 main 且遠端分支被刪，開工時的 stack 決策（當時正確：測試檔僅存在該分支）一小時內過時。由 reviewer 以 S3 發現，需 stash→reset→pop rebase 並重跑全套驗證（860 tests）。

## 3. 改進提案

- **L0812_0822-1**：`target=.claude/skills/develop/references/code-cycle.md | sketch=Phase 4 與 Phase 8 開頭各加一次 git fetch + 基底存活檢查（parent 分支已合併/刪除 ⇒ 先 rebase 再進行）| status=proposed`。本 run 靠 reviewer 偶然發現漂移；phase 邊界一次 fetch 是零成本的系統性保證。
- **L0812_0822-2**：`target=.claude/skills/develop/SKILL.md | sketch=§3 dead-agent 規則補處方例外 — 中斷 agent 的編輯已完整且與 orchestrator 處方逐字一致（小 diff）時，可由 orchestrator 驗 diff 取代重派，記入 Fix Cycle Log | status=proposed`。淨增一句的理由：避免對已完成的工作整段重派（本 run 實際省下一次 coder 派遣）。
- **L0812_0822-3**：`target=.claude/rules/test.md | sketch=註明 worker 的 pnpm test -- <path> 不會縮小範圍（vitest run 腳本吞掉參數），focused run 須用 npx vitest run <path> | status=proposed`。tester 本 run 實測發現；AGENTS.md 的 extension 寫法（`pnpm test -- tests/unit/crypto`）在 worker 不適用，易誤導。
- **E1**：stacked-branch 任務開工前除 `gh pr list` 外，也檢查父分支最後 commit 時間與是否為活躍 worktree；run 中每個 phase 邊界順手 `git fetch` — 上游隨時可能合併（本 run 即發生）。
- **E2**：非互動 session 的長派遣（>5min）前先確認剩餘額度風險；coder 這類「處方已釘死」的小改動可縮小 prompt 要求（僅套用處方＋回報），降低中斷損失面。

## 4. Skill / Agent 增減建議

- 無增減。reviewer 的 Observations（mutation 防護、呼叫端逐一追查）與 security-auditor 的 Ruled-out 段本 run 皆直接支撐決策，維持現配置。

## 5. 三軸 KPI

- speed: bottleneck_seconds: 996; serialized_legs: 0
- precision: requirement_rounds: 0; critical_count: 0; suggestion_adopted/proposed: 3/3; rework_rt: 0
- context: dispatches: 5; self_reads: 5; top_consumer: agent dispatches（security-auditor 118k + reviewer 91k tokens）; lever: reviewer prompt 附上預算好的 diff 與呼叫端清單，削減其 30 次 tool uses 的探索段
- meta: patch_proposed: 3

# Retro — 0827_1442(fix/badge-borrow-sanitize)

## 1. Run 概要

| 項目                  | 值                                                                                                                       |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| Intent route          | CODE(frontend,bug fast-path)                                                                                             |
| Agent dispatches      | 6(coder 1、tester 2、reviewer 1、security-auditor 2;其中 2 次為 API 截斷後重派)                                          |
| Fix Cycle rounds      | 1(review 一輪即 PASS,0 CRITICAL / 0 SUGGESTION)                                                                          |
| Gate rounds           | 需求確認 0(fast-path 併入最終報告)、verify-before-test 0、SUGGESTION 決策 0、最終 AskUserQuestion 1(retro + commit 併批) |
| Diff stat             | 2 files,+512 / −4(`extension/src/content/index.ts`、`extension/tests/unit/content/pendingBadge.test.ts`)                 |
| Wall-clock bottleneck | tester 重派後成功輪:約 1734s                                                                                             |
| 特殊決策              | 分支疊在未合併的 `fix/borrow-list-boundary-validation`(44aee30)上取得 sanitizer,避免重複實作                             |

## 2. 卡點與摩擦

- **G1 — subagent 被 API 錯誤截斷 ×2**:tester 首輪(session limit)與 security-auditor 首輪(response stopped arriving)皆中途死亡。依 SKILL.md §3 以原 prompt + 一行截斷註記重派全新 agent,兩次皆成功;重派前先 `git status` 確認無半成品殘留(tester 首輪幸無寫檔)。損耗約 25–30 分鐘 + 約 16 萬 subagent tokens。
- **G2 — 外部 prettier churn 污染 working tree ×2**:tester 回報其 tool call 之外有東西對全 repo 跑了 `prettier --write`,弄髒 `extension/public/manifest.json` 與 `extension/tests/e2e/fixtures/mock-readmoo.html`(兩者以非 prettier 格式入庫),由 tester 還原;稍後 orchestrator 又發現 63 個 `.claude/**` 檔案 status 顯示 M(內容零差異、純 stat/EOL churn),以 `git restore .claude/` 清除。來源未確認(疑似本機 PostToolUse 格式化 hook)。若未清除,會直接混入 commit。
- **G3 — sanitizer 相依落在未合併分支**:目標函式 `sanitizeBorrowRequests` 只存在於 `fix/borrow-list-boundary-validation`,main 上沒有。以 stacked branch 解(reset --hard 到該分支後開工),無 rework,但 PR base 選擇被前置相依綁定,commit gate 已向使用者揭露。

（U#:本 run 無使用者中途修正。）

## 3. 改進提案

- **L0827_1442-1**:`target=.claude/skills/develop/references/code-cycle.md | sketch=Phase 8 git add 前加一行:先 git status --short 全樹,還原與本任務無關的格式化/EOL churn(外部 prettier、hook 所致),再 stage | status=proposed`
- **L0827_1442-2**:`target=.claude/skills/develop/SKILL.md | sketch=§3 dead-agent 重派規則補一句:重派前先 git status 檢查半成品殘留並還原,再以原 prompt 重派 | status=proposed`
- **E1(執行槓桿)**:tester agent 內部全套件跑了兩次(綠一次 + 高負載 flake 一次再單檔重跑),下次 tester prompt 明示「全套件跑一次即可;失敗檔單獨重跑判定 flake」可省約 4–5 分鐘。
- **E2(執行槓桿)**:lint + E2E typecheck + reviewer dispatch 併入同一訊息平行執行(本次已做)——維持為慣例,省一條序列腿。
- **E3(執行槓桿)**:stacked 相依場景下,security-auditor 的 `base_ref` 指向疊層基底分支而非 origin/main,避免重審前置 PR 的 diff(本次已做,效果良好)。

## 4. Skill / Agent 增減建議

- 無新增/移除建議。現有 coder/tester/reviewer/security-auditor 分工在本 run 均正確發揮;reviewer 依 change-triage 把全部候選壓在 P2 不提出,行為符合設計。

## 5. 三軸 KPI

```
speed.bottleneck_seconds: 1734
speed.serialized_legs: 0
precision.requirement_rounds: 0
precision.critical_count: 0
precision.suggestion_adopted/proposed: 0/0
precision.rework_rt: 0
context.dispatches: 6
context.self_reads: 5
context.top_consumer: subagent dispatches(約 58 萬 tokens,含 2 次截斷損耗)
context.lever: tester 全套件只跑一次(E1)
meta.patch_proposed: 2
```

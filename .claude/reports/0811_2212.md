# Retro 0811_2212 — worker route-layering lint 護欄

## 1. Run 概要

| 項目            | 值                                                                                                                                  |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| Intent route    | CODE(backend 單一 scope)                                                                                                            |
| Dispatch 總數   | 4(coder ×2、reviewer ×1、security-auditor ×1;tester 0 = N/A:純 lint config/註解/文件,驗收即負向 lint 測試)                          |
| 平行化          | reviewer + security-auditor 同訊息平行;coder 兩輪依相依序列                                                                         |
| Fix Cycle 輪數  | 1(SUGGESTION ×3 全採用;prescription exception 自核代替 re-review)                                                                   |
| Gate 輪數       | 需求確認 0、verify-before-test 0(無人值守,由 orchestrator 代跑使用者指定的負向測試)、SUGGESTION 決策 0(自主 TL 判斷)、commit gate 1 |
| Diff stat       | 3 檔,+35 / −2                                                                                                                       |
| Wall-clock 瓶頸 | security-auditor 481s(reviewer 446s、coder#1 348s、coder#2 163s)                                                                    |
| Security scan   | PASS(0 CRITICAL / 0 WARNING / 1 INFO)                                                                                               |

## 2. 卡點與摩擦

- **G1 — 任務前提與 repo 實況不符**:請求聲稱「目前 `worker/src/routes/` 已無任何 sibling import,正是加規則的乾淨時點」,但 `worker/src/routes/user.ts:13` 的 `writePublicSnapshot` 已知債在 origin/main 上仍存在(`.claude/rules/backend.md` Layering caveat 也明文記錄)。開工前的 grep 核對攔到了;若直接照做,lint 會紅、驗收不可能達成。以點對點 `eslint-disable` 豁免(最小範圍)化解,債務清償另立獨立任務,未耗費任何 user round-trip。
- **G2 — Bash 工作目錄延續**:一次 `cd worker` 失敗(前一呼叫已在 `worker/`),損失一個 tool call。已以 `pwd` 前置確認收斂。

## 3. 改進提案

- **L0811_2212-1**:`target=.claude/skills/develop/references/code-cycle.md | sketch=Phase 1 intake 增列:請求中「已完成/已乾淨」類的 repo 狀態前提必須先以程式碼核對(grep/read),不符時走最小範圍處理＋獨立任務,不靜默採信 | status=proposed`。淨增長理由:AGENTS.md 的「verify in code first」只涵蓋回答問題,未涵蓋任務前提;本 run 的驗收條件整組建立在錯誤前提上。
- **L0811_2212-2**:`target=.claude/skills/develop/SKILL.md | sketch=§2 補無人值守(autonomous)run 的 gate 政策:reviewer 已開處方的 🟢 SUGGESTION 可自主採用並在 Fix Cycle Log 透明記錄;需求確認/verify-before-test 由 orchestrator 代跑可自證的驗收;終局決策(commit/retro)批次成單一 AskUserQuestion | status=proposed`。淨增長理由:現行 §2 的 stop discipline 假設使用者在線,無人值守時無所適從。
- **E1(執行槓桿)**:加 lint/guard 類規則時,先證明規則會觸發(在豁免前跑一次、看到預期 error),再中和它 — 本 run coder 以此避免「靜默不匹配的 pattern 假通過驗收」。
- **E2(執行槓桿)**:ESLint pattern 語意用 ESLint 實際採用的 matcher(`ignore` 套件,gitignore 語意、`*` 跨層)實測,不用 minimatch 直覺推論 — 本 run 因此砍掉冗餘的深層 pattern、又補上真正需要的 `**/routes/*`。

## 4. Skill / Agent 增減建議

- 無增減。小 diff 時 reviewer + security-auditor 平行(SKILL.md §3 既有規定)實測有效,兩者獨立佐證了同一條繞道(S2),提高了自主採用的信心。
- Slim 候選(供 /distill 權衡):security-auditor 對「純工具鏈/文件 diff」仍複驗了全部 Inv-1..5 並跑完整測試套件(758 條)— 對 src 僅一行註解的變更偏重;可在 dispatch prompt 收窄驗證面。

## 5. 三軸 KPI

- speed: `bottleneck_seconds: 481`、`serialized_legs: 0`
- precision: `requirement_rounds: 0`、`critical_count: 0`、`suggestion_adopted/proposed: 3/3`(另 2 項範圍外轉獨立任務)、`rework_rt: 0`
- context: `dispatches: 4`、`self_reads: 1`(worker/eslint.config.js;另 2 次 grep、diff 檢視若干)、`top_consumer: security-auditor dispatch(104k tokens)`、可削減槓桿:工具鏈-only diff 收窄 security-auditor 的驗證面
- meta: `patch_proposed: 2`

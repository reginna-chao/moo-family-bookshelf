# Run Retrospective — 0819_1124

## 1. Run 概要

| 項目                  | 值                                                                                                                                             |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| Intent route          | CODE(bug fast-path,frontend scope,雙 surface:extension + pwa)                                                                                  |
| 任務來源              | fix/public-share-code-lookup 審查時 reviewer 鄰域掃描發現的 2 個範圍外同類問題(後端可控 status 索引查表 → render crash)                        |
| Dispatch 總數         | 7 — coder ×2、tester ×2(其中 1 次中途死亡)、reviewer ×2、security-auditor ×1                                                                   |
| Fix Cycle rounds      | 2(R1:0 CRITICAL / 3 SUGGESTION;R2 focused:PASS,0/0)                                                                                            |
| Gate rounds           | 需求確認 0(fast-path,需求由前 PR reviewer 處方完整釘死);verify-before-test 0(併入 fast-path);SUGGESTION 決策 1(AskUserQuestion,採 TL 建議全修) |
| Diff stat             | 4 files, +201 / −25(生產碼 2 檔 +77/−25,測試 2 檔 +149)                                                                                        |
| Wall-clock bottleneck | security-auditor 763s(次高:tester#1 612s)                                                                                                      |
| Security verdict      | PASS WITH WARNINGS(變更集本身 0C/0W;範圍外揪出 VerificationSettings.tsx:145 同類活體 → 已掛任務籤)                                             |
| 範圍外產出            | 任務籤 ×4(FamilySettings flaky、BorrowPage 拆分、listBorrowRequests 邊界驗證、VerificationSettings 同類漏洞)                                   |

## 2. 卡點與摩擦

- **G1(stop-discipline 缺失)**:orchestrator 在展示 coder diff 後結束回合,未附 Stop Block 也未接續派 tester,使用者需手動催「繼續,照原本的測試計畫跑 tester」。違反 SKILL.md §2「silent stop 是 defect」。
- **G2(agent 中途死亡 + 復原路徑偏離)**:tester#2(S2+S3)因 session limit 在「編修完成、尚未跑測試」時被切斷。code-cycle 規則要求 re-dispatch fresh agent,實際做法是 orchestrator 以 `git diff` 逐行核對編修完整性 + 自跑兩個測試檔驗證,省下一次 dispatch。結果正確,但屬規則未涵蓋的情境(死亡時工作產物已完整)。
- **G3(環境摩擦,小)**:Bash 工作目錄跨呼叫殘留(停在 `pwa/`),`git add` 以 repo-root 相對路徑失敗一次;重下 `cd <worktree root> && …` 解決。
- **U1**:「Continue from where you left off」/「Try again」×2 —— 均為 session limit 中斷後的機械性重試,非需求修正。本 run 無任何需求層面的使用者更正。

## 3. 改進提案

- **L0819_1124-1**:`target=.claude/skills/develop/SKILL.md`(§3 dead-agent 規則)| sketch=死亡 agent 的檔案編修若已完整(git diff 可核對 + 驗證命令可跑),允許 orchestrator 直接驗證工作產物取代 re-dispatch;僅在編修不完整或無法驗證時才強制 fresh re-dispatch | status=proposed
- **L0819_1124-2**:`target=AGENTS.md`(Build & Development Commands)| sketch=「Tests (extension, specific): `pnpm test -- tests/unit/crypto`」已失效——pnpm 會吃掉 `--`,實跑全套;應改為 `pnpm vitest run <path>`(tester#1 實測確認)| status=proposed
- **E1**:tester 跑過全套後 orchestrator 又重跑一次全套(各 ~60s);當 orchestrator 必定在 gate 前自跑全套時,agent prompt 可明示「只跑目標檔,全套由 orchestrator 收尾」——本 run 後半已如此做,前半未做。
- **E2**:Windows 下跨 Bash 呼叫的 cwd 會殘留:所有 git/驗證命令一律以 `cd <絕對 worktree root> && …` 開頭,不依賴前次呼叫的目錄。

## 4. Skill / Agent 增減建議

- 無增減。本次 coder/tester/reviewer/security-auditor 分工與並行(S1 coder ∥ S2S3 tester;R2 review ∥ security scan)運作良好;security-auditor 的鄰域掃描再度證明價值(連兩個 PR 各揪出一個範圍外同類活體),維持現配置。

## 5. 三軸 KPI

- speed: bottleneck_seconds: 763
- speed: serialized_legs: 0
- precision: requirement_rounds: 0
- precision: critical_count: 0
- precision: suggestion_adopted/proposed: 3/3
- precision: rework_rt: 0
- context: dispatches: 7
- context: self_reads: 2
- context: top_consumer: subagent dispatches(7 次,合計 ~598k subagent tokens,security-auditor 單次 ~127k 最高)
- context: 可削減槓桿: 見 E1(全套測試重複執行)
- meta: patch_proposed: 2

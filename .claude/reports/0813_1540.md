# Retro — 0813_1540 · 移除 join 端點 owner-facing per-userId 速率計數器

## 1. Run 概要

| 項目                  | 值                                                                                                               |
| --------------------- | ---------------------------------------------------------------------------------------------------------------- |
| Intent route          | CODE / backend                                                                                                   |
| Dispatch 數           | 5(coder 1、tester 2〔首次因 session limit 中斷、重派 1〕、reviewer 1、security-auditor 1；reviewer+auditor 並行) |
| Fix Cycle rounds      | 0(無 CRITICAL；2 SUGGESTION 皆於乾淨重建中消解，非經 agent 修復輪)                                               |
| Gate rounds           | 需求確認 1 / verify-before-test 1（0 重試）/ SUGGESTION 決策 0                                                   |
| Diff stat             | 6 files, +101 / −26                                                                                              |
| Wall-clock bottleneck | tester 重派成功 ~500s（slowest dispatch）                                                                        |

## 2. 卡點與摩擦

- **G0813_1540-1（最大，自身誤判）**：worktree base 落後被誤判為「另一任務的未提交汙染」。我先跑 `git diff origin/main` 看到 14 個檔案，其中 8 個(kv/schema、publicShelf、mockKv、rateLimit.test、刪除的 2 份 report…)與本任務無關，遂發出 blocker + AskUserQuestion(隔離方式)。**實際**：worktree base 停在 #103，origin/main 已前進到 #109；那 8 個檔案是上游 **#107 / #108**(caller-list sync、KV 60s TTL clamp)合併進 origin/main 的結果，不是任何人的未提交工作。直到跑 `git diff HEAD`(對 worktree 自己的 base)才確認 working tree **只改了我的 6 個檔**。證據：`git diff --stat HEAD` = 6 檔；`git log HEAD..origin/main` = #107/#108/#109。代價：約 5 次 git 診斷、一次假 blocker stop、一次 AskUserQuestion round。
- **G0813_1540-2（連帶）**：reviewer 的 finding #2(`chargePerUserRateLimit` 的 `Math.max` clamp「被移除」)與 security-auditor 的 changes-overview 皆為 stale-base 假警報 —— 兩者以 `base_ref: origin/main` 比對落後的 worktree，把上游 #108 新增的 clamp 讀成「本次移除」。乾淨 base 上 clamp 原封存在。
- **G0813_1540-3（處置正確）**：tester 首次 dispatch 因 session limit(API error)中斷且未寫檔；以 `git status` 確認 `worker/tests/` 無殘留後重派新 agent 成功，未沿用死掉 agent 的部分輸出。
- **U#**：無使用者對產出的糾正；本次摩擦全由自身 base 誤判引起。

## 3. 改進提案

**Flow-structural**

- `L0813_1540-1` | target=`.claude/skills/develop/references/code-cycle.md`(Phase 0 Branch Preflight) | sketch=worktree 出現非預期變更時，先 `git diff HEAD`(對自己 base)區分「我的編輯」vs「base 落後 origin/main 的上游漂移」，並 `git log HEAD..origin/main` 看上游加了什麼，再下「汙染」結論；只比 `git diff origin/main` 會把已合併上游 commit 誤判為他人未提交汙染 | status=proposed
- `L0813_1540-2` | target=`.claude/skills/develop/references/code-cycle.md`(Phase 4/6 dispatch note) | sketch=派 reviewer / security-auditor 帶 `base_ref: origin/main` 前，若 worktree base 落後 origin/main，改用 merge-base 或先 rebase，否則上游漂移(如 #108 clamp)會被當成本次變更被誤標 | status=proposed

**執行槓桿（no flow gap）**

- `E0813_1540-1`：任何 worktree 任務 Phase 0 先跑 `git log HEAD..origin/main --oneline`；非空即代表 base 落後，先 rebase 或明確記錄漂移，讓後續 `git diff origin/main` 讀得乾淨。
- `E0813_1540-2`：agent 因 session limit 死掉時，重派前先 `git status` 確認未寫檔 —— 本次做對，維持為習慣。

## 4. Skill / Agent 增減建議

- 不新增、不移除。唯一調整併入 `L0813_1540-2`：在 coder / reviewer / security-auditor 的 dispatch 樣板加一行 merge-base 提醒。使用者角度收益：下次遇到 stale worktree 不會再走一次假 blocker + 假 finding。

## 5. 三軸 KPI

- `speed`: bottleneck_seconds=500 · serialized_legs=0（reviewer+auditor 已並行）
- `precision`: requirement_rounds=1 · critical_count=0 · suggestion_adopted/proposed=1/2（S1 caller-list 修好；S2 clamp 為假警報作廢）· rework_rt=1（乾淨 worktree 重建，由 stale-base 誤判觸發）
- `context`: dispatches=5 · self_reads=14（中性計數）· top_consumer=汙染誤判的 git-diff 診斷 + reviewer/auditor 長報告 · cuttable=Phase 0 先 `git diff HEAD` 可省掉整段誤判
- `meta`: patch_proposed=2

# Retro — 0819_1633（fix/pwa-recovery-join-logout）

## 1 Run 概要

| 項目             | 值                                                                                                                                                             |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Intent route     | CODE（frontend / pwa）                                                                                                                                         |
| Dispatch 總數    | 6（coder ×2、tester 角色 ×2、reviewer 角色 ×1、security-auditor 角色 ×1）                                                                                      |
| Fix Cycle rounds | 1（0 CRITICAL；4 SUGGESTION，採納 2 = S3+S4；S4 以 prescription 例外自核免複審）                                                                               |
| Gate rounds      | 需求確認 0（bug fast-path，任務由稽核報告預先錨定）；verify-before-test 0（autonomous session 折疊至終局呈現）；SUGGESTION 決策 1（AskUserQuestion 批次 3 題） |
| Diff stat        | 4 檔，+564 / −66（產品碼 2 檔 +218/−23，測試 2 檔 +346/−43）                                                                                                   |
| Wall-clock 瓶頸  | security-auditor dispatch，604s                                                                                                                                |

備註：本 run 中途 tester / reviewer / security-auditor 專用 agent type 一度自 registry 消失，改派 general-purpose 並要求先讀 `.claude/agents/<role>.md` 角色定義——功能上完全等效。

## 2 卡點與摩擦

- **U1**（使用者中斷，非更正）：「I hit my usage limit while you were working, but it has reset now. Please continue from where you left off.」——S4 coder 與 S3 tester 兩個 dispatch 被 usage limit 切斷。
- **G1** 任務簡報漂移：稽核備註引用的 `pwa/src/utils/joinErrorMessages.ts`（`JOIN_BLOCKED_MESSAGES`）與 `renderWithFailedJoin` helper 屬未合併分支（fix/kicked-member-rejoin），main 上不存在；已依備註自帶的「base on whatever main holds and adapt」警語改由 worker 實際錯誤碼推導終端碼集合。證據：grep `JOIN_BLOCKED_MESSAGES` 零命中。
- **G2** 中斷後的救援路徑：兩個被切斷的 agent 其編輯已完整落地（coder 斷在驗證、tester 斷在跑測試），採「檢視落地編輯 → 自行驗證」而非按 SKILL.md §3 字面重派全新 agent，省一整輪 dispatch；全套驗證綠燈證明救援正確。
- **G3** 基底漂移：run 中 #129 併入 origin/main，worktree 落後一個 commit；無檔案重疊，fast-forward 後重驗全綠。
- **G4** 環境摩擦：Bash tool PATH 無 git（需改 PowerShell）；usage-limit 中斷後 PowerShell cwd 重置，一次誤在 repo 根跑了全 workspace 驗證（無害，反成加值驗證）。

## 3 改進提案

- **L0819_1633-1**：`target=.claude/skills/develop/SKILL.md | sketch=§3 agent 中斷規則細化——中斷後先檢查工作區已落地的編輯是否完整（diff 對照派工規格），完整則直接驗證採納，僅不完整才重派；「never build on partial output」的本意是別信任片段輸出文字，不是丟棄可驗證的已完成編輯 | status=proposed`
- **L0819_1633-2**：`target=.claude/skills/develop/SKILL.md | sketch=§3 加 fallback 一行——角色 agent type 不可用時改派 general-purpose 並令其先讀 .claude/agents/<role>.md 依角色定義執行 | status=proposed`
- G1 不提升：稽核備註已自帶「may not be merged — adapt」警語，本次無誤判、無返工。
- **E1**：Windows 環境 git 一律走 PowerShell tool（Bash tool 的 sh PATH 無 git）。
- **E2**：PowerShell 呼叫不依賴上一輪 cwd（session 重置即遺失）；關鍵指令帶絕對路徑或先 cd 並驗證。
- **E3**：小 diff 時 reviewer 與 security-auditor 並行（本次省約 400s wall-clock）已驗證可行，維持此習慣。

## 4 Skill / Agent 增減建議

- 無增減。general-purpose + 角色定義檔的代打模式運作良好（見 L-2，建議文件化為 fallback 而非新增 agent）。

## 5 三軸 KPI

```
speed.bottleneck_seconds: 604
speed.serialized_legs: 0
precision.requirement_rounds: 0
precision.critical_count: 0
precision.suggestion_adopted/proposed: 2/4
precision.rework_rt: 0
context.dispatches: 6
context.self_reads: 9
context.top_consumer: agent dispatches（六次合計約 68 萬 subagent tokens，最大宗 security-auditor 16.3 萬）
context.lever: reviewer/auditor 簡報瘦身——blast-radius 檔案清單讓 agent 自 grep，簡報只給行為規格
meta.patch_proposed: 2
```

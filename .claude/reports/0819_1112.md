# Run Retrospective — 0819_1112

## 1. Run 概要

| 項目                | 值                                                                                                                                                      |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Intent route        | CODE（deflake：延續 #121/#127）                                                                                                                         |
| Agent dispatch 總數 | 12（Explore 2、coder 1、tester 7、reviewer 1、security-auditor 1）                                                                                      |
| tester 7 次的組成   | 首發 2（session 限額陣亡、零編輯）＋重派 2（1 個早期 stall、1 個無完成紀錄）＋三發 2（編輯全落地、於自跑壓測時 stall）＋ Fix Cycle 1（S1+S5，正常完成） |
| Fix Cycle 輪數      | 1（CRITICAL 0；SUGGESTION 5 → 採納 2、跳過 3）                                                                                                          |
| Gate 輪數           | 需求確認 0、verify-before-test 0（自主模式，改為事後報告）、SUGGESTION 決策 0（TL 建議逕行）、最終 AskUserQuestion 1（retro＋commit 合併一次問）        |
| Diff stat           | 11 files, +292 / −109（production 3、tests 8）                                                                                                          |
| Wall-clock 瓶頸     | extension 根因分析 Explore ≈ 807s（最慢單一 dispatch）                                                                                                  |
| 驗收證據            | 兩套件同時跑 ×3 輪全綠（ext 2471、pwa 999）；S1 絆線測試經 mutation check 證實                                                                          |

## 2. 卡點與摩擦

- **G1（環境）**：首發兩個 tester 同時被 API session 限額終止（重置 12:50am Taipei）。全有或全無——磁碟零編輯，重派 verbatim prompt 即可，無需清理。
- **G2（agent 內壓測 → watchdog stall ×3）**：重派與三發的 tester 在「自行重複壓測驗證」階段觸發 600s 無進度 watchdog 而被判 failed，但其編輯早已完整落地。單一長時間 `pnpm test` / 壓測迴圈在 agent 內沒有進度輸出，是 stall 判定的直接成因。恢復成本：orchestrator 需以 `git status` + diff 逐檔核對後接手集中驗證。
- **G3（正向確認）**：鏡像檔（PublicShareDialog.writeSync ×2）指派給單一 agent 一次改完，hunk 逐字一致、零漂移——複驗了 0817_1800 報告的 G3 教訓，本報告不再重複提案。
- **U#**：無（使用者僅於最終 gate 回覆，無中途修正）。

## 3. 改進提案

- **L0819_1112-1**：`target=.claude/skills/develop/references/code-cycle.md | sketch=tester agent 的 verify 範圍止於 targeted + 單次全套；重複/高併發壓測驗收由 orchestrator 於 agent 返回後集中執行（agent 內長跑會觸發 600s watchdog 誤判 failed）| status=proposed`
- **L0819_1112-2**：`target=.claude/rules/test.md | sketch=在 Anti-Drift Rules 補一條：vi fake timers 安裝期間禁用 waitFor/findBy*（RTL 偵測不到 vi 假時鐘，waiter 輪詢凍結時鐘直到 testTimeout——是 30s 整數逾時的唯一機制；絆線：檔案層 afterEach(useRealTimers)）| status=proposed`
- **E0819_1112-1**：subagent 被判 failed/stalled 後，先 `git status` 核對磁碟完成度再決定重派——本次三發 tester 名義上 failed 但工作已完整，省下第四次派工。
- **E0819_1112-2**：Explore 根因調查的 prompt 要求「結論＋逐項證據行號」即可，明確要求壓縮輸出長度；本次兩份診斷極詳盡但各 12 萬＋ 18 萬 tokens，是最大單類 context 消耗。

## 4. Skill / Agent 增減建議

- 無增減。tester agent 定義可考慮直接內建 L0819_1112-1 的 verify 範圍上限（由 /distill 決定落點在 skill 參考或 agent 定義擇一，勿兩處重複）。

## 5. 三軸 KPI

```
speed.bottleneck_seconds: 807
speed.serialized_legs: 1
precision.requirement_rounds: 0
precision.critical_count: 0
precision.suggestion_adopted/proposed: 2/5
precision.rework_rt: 0
context.dispatches: 12
context.self_reads: 4
context.top_consumer: Explore 診斷報告（2 份合計 >300k subagent tokens）
context.cuttable_lever: Explore prompt 明定輸出壓縮（見 E0819_1112-2）
meta.patch_proposed: 2
```

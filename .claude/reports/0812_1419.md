# Retro 報告 — 0812_1419

Run:publicShelf 四個寫入 handler 補 per-userId 限流(fix/public-shelf-user-ratelimit)。

## 1 Run 概要

| 項目            | 值                                                                                   |
| --------------- | ------------------------------------------------------------------------------------ |
| Intent route    | CODE(backend,fast-path:需求為前次稽核已釘死的處方)                                   |
| Dispatch 總數   | 7(coder 2、tester 2、reviewer 2、security-auditor 1)                                 |
| Fix Cycle 輪數  | 1(S2–S5 一輪套用,focused re-review PASS)                                             |
| Gate 輪數       | 需求確認 0 / verify-before-test 0 / SUGGESTION 決策 0(自主 session,決策併入期末報告) |
| Diff stat       | 6 檔:+限流實作與 429 宣告、+13 測試案例、4 檔註解/文件同步(`git diff --stat` 見分支) |
| Wall-clock 瓶頸 | 初輪 tester dispatch 約 750 秒                                                       |
| Security scan   | PASS WITH WARNINGS(0 CRITICAL;3 WARNING 均屬既有/前端,已建 2 個追蹤 chip)            |

## 2 卡點與摩擦

- **G1 — origin/main 於 run 中途前進**:Phase 0 preflight 時 `origin/main..HEAD` 為空,但 run 進行中 #100 合入;首輪 reviewer(O1)發現 `git diff origin/main` 會夾帶 #100 的反向 diff,orchestrator 才補 fast-forward。若 reviewer 未察覺,PR diff 會夾帶無關回退。
- **G2 — 任務前提被審查推翻**:進來的需求宣稱 publicShelf 是「唯一」缺 per-userId 限流的寫入模組;reviewer S1 逐一核對推翻(`verify.ts` 0 處、`family.ts` 僅 join 有)。修正本身仍正確,但 PR 敘述須由「關閉缺口」降級為「收斂 public-shelf 寫入面」,並衍生追蹤任務。
- **G3 — orchestrator brief 含未驗證事實**:給 coder 的 brief 寫「hourly windows ⇒ 7200s」概括所有 scope,實際 `bookshelf`/`borrow-*` 是 60s window;coder 對照原始碼後自行修正。brief 的錯誤事實若被照抄就進文件。
- **G4 — fix 輪 coder→tester 被迫序列化**:S3 抽常數時才決定 export `SHELF_WRITE_RATE_LIMIT` 供測試 import,tester 必須等 coder 完成,兩個 file-disjoint dispatch 無法平行。
- U#:無(自主 session,使用者僅於期末 gate 決策)。

## 3 改進提案

- **L0812_1419-1**:target=`.claude/skills/develop/references/code-cycle.md` | sketch=Phase 4 review 前(或 Phase 8 commit 前)強制 `git fetch origin` 並 ff/rebase 至 origin/main,不依賴 reviewer 偶然發現 | status=proposed
- **L0812_1419-2**:target=`.claude/rules/test.md` | sketch=prod-mode 限流測試需注意與 per-IP 60/min 計數器共用額度(`prodRequest` 無 cf-connecting-ip 時全落在 `ratelimit:unknown:*`);大量擴充時以每 case 唯一 IP header 隔離 | status=proposed(本 run reviewer O5 與 tester 各自獨立指出同一耦合)
- **L0812_1419-3**:target=`.claude/skills/develop/SKILL.md` §2 | sketch=補一行自主(非互動)session 的 stop 規則:決策 gate 批次移到期末報告、CRITICAL 照常自動修、🟢/🟡 SUGGESTION 可先套用並記錄供回退 | status=proposed
- **E0812_1419-1**:「唯一/only」這類排他性宣稱在寫入 prompt 或 PR 敘述前,先用一次 grep 機器驗證(本 run 由 reviewer 事後推翻,成本高於事前一個 grep)。
- **E0812_1419-2**:orchestrator brief 不斷言未驗證的數字事實;改為「請對照原始碼驗證」的指示(G3 靠 coder 自行核對才沒把錯誤寫進文件)。
- **E0812_1419-3**:fix 輪若預期測試要 import 新 export,在初輪 coder brief 就指定 export,保住 coder/tester 平行度(G4)。

## 4 Skill / Agent 增減建議

- 無增減。security-auditor 的 WARNING → spawn_task 追蹤 chip 的銜接運作良好(2 個 chip:verify/family 限流缺口、PublicShareDialog 吞 429),維持現狀。

## 5 三軸 KPI

- speed: bottleneck_seconds: 750
- speed: serialized_legs: 1
- precision: requirement_rounds: 0
- precision: critical_count: 0
- precision: suggestion_adopted/proposed: 4/5
- precision: rework_rt: 0
- context: dispatches: 7
- context: self_reads: 4
- context: top_consumer: agent dispatches(七次共約 79 萬 subagent tokens)
- context: lever: 初輪 brief 即指定測試所需 export,fix 輪 coder/tester 可平行(見 E-3)
- meta: patch_proposed: 3

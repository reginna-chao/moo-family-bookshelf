# Retro 0811_1805 — 驗證閘門抽層（refactor/extract-verification-gate）

任務：把驗證閘門從 `worker/src/routes/verify.ts` 抽成 `worker/src/services/verification.ts`（純搬移、零行為變更），`hashSecret` / `timingSafeEqual` 落腳 `worker/src/utils/crypto.ts`。

## 1. Run 概要

| 項目           | 數值                                                                                 |
| -------------- | ------------------------------------------------------------------------------------ |
| Intent route   | CODE                                                                                 |
| Agent 派發     | 4（coder 1 / tester 1 / reviewer 1 / security-auditor 1）                            |
| 並行派發       | 2 組（tester ∥ orchestrator 自驗腳本；reviewer ∥ security-auditor）                  |
| Fix Cycle 輪數 | 1（CRITICAL 0；SUGGESTION 3 → 文件面採納 2、跳過 1）                                 |
| Gate 輪數      | 需求確認 0 / verify-before-test 0 / SUGGESTION 決策停點 0（見 G3）                   |
| Diff           | 8 檔，+546 / −525                                                                    |
| 最慢派發       | coder 484s（tester 96s、reviewer 359s、auditor 382s）                                |
| 驗收           | typecheck / lint / test 全綠；758 測試 = 基線；覆蓋率 92.22% = 基線；新檔兩個皆 100% |

## 2. 卡點與摩擦

- **G1 — 自驗腳本兩次重試。** 第一版逐字元比對用 inline `node -e` 多行腳本，在 Windows/bash 引號下靜默無輸出；改寫成 scratchpad 檔後又漏算原檔第 35 行（`export const verifyRoutes`）造成一次 FAIL。兩輪皆為 orchestrator 自身失誤，與 agent 無關。
- **G2 — 規則出廠即被違反。** 我把「route 不得 import sibling route 的邏輯」寫進 `.claude/rules/backend.md` 時未先 grep 全 repo 找反例；reviewer S1 抓到 `worker/src/routes/user.ts:13` import `routes/publicShelf.ts` 的 `writePublicSnapshot` 正是現存違例。已於 run 內改為誠實版本（明列已知殘留 + 開背景任務 chip 追蹤下沉），但一次 10 秒的 grep 本可在寫規則前就攔下。
- **G3 — SUGGESTION 決策未經 AskUserQuestion 停點。** 三項建議（S1 規則違例、S2 services→middleware 相依、S3 randomHex 收斂）全數逾越使用者明訂的「純搬移」硬界線，我自行判定：文件面處理 S1/S2、跳過 S3，事後在報告中揭露。與「決策點一律用選項元件」的既有偏好有張力；本次風險低（界線是使用者自己劃的），但若使用者其實想順手採 S3 就需要事後補做。

U#：無（使用者指令一次到位，run 中無更正）。

## 3. 改進提案

- **L0811_1805-1**：`target=.claude/skills/develop/references/code-cycle.md | sketch=新增「機器可驗證純搬移」fast path——驗收完全機械（既有測試網 + 逐字元比對）時，verify-before-test 停點可由 orchestrator 獨立 byte-identity 檢查取代，不需人工驗證停點 | status=proposed`
- **L0811_1805-2**：`target=.claude/skills/develop/references/code-cycle.md | sketch=SUGGESTION 處理補一條：使用者已預先明訂硬範圍界線時，逾界建議預設「跳過 + spawn_task chip 追蹤」可不設停點，界內建議照常停 | status=proposed`（即把 G3 的做法制度化；若不採納則反向明確化「仍須停點」）
- **不提升（G2）**：寫規則前先 grep 反例——屬執行紀律非流程結構，列 E2 即可。

執行槓桿（E#，非流程修改）：

- **E1**：多行驗證腳本一律直接寫 scratchpad 檔執行，不用 inline `node -e`（Windows 引號會靜默吞輸出，G1 實證）。
- **E2**：在 rules 檔寫下任何「絕不 / 一律」型規則前，先 grep 全 repo 找現存反例，違例要嘛一併修、要嘛明列為已知殘留。
- **E3**：收尾驗證用單次 `pnpm test:coverage` 同時覆蓋「測試綠 + 覆蓋率持平」，省一趟完整測試跑。

## 4. Skill / Agent 增減建議

- 無增減。reviewer 與 security-auditor 本次皆自行做了獨立逐字元複核（不採信 PR 描述），品質高，維持現狀；兩者並行派發 + opus override 的組合值得沿用於安全敏感重構。

## 5. 三軸 KPI

```
speed:
  bottleneck_seconds: 484
  serialized_legs: 0
precision:
  requirement_rounds: 0
  critical_count: 0
  suggestion_adopted/proposed: 2/3
  rework_rt: 0
context:
  dispatches: 4
  self_reads: 5
  top_consumer: coder 派發（110k subagent tokens；主 session 內最大單項為 841 行 verify.ts 全讀）
  lever: 收尾改單次 pnpm test:coverage（見 E3）
meta:
  patch_proposed: 2
```

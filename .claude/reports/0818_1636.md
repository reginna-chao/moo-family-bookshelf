# Retro 0818_1636 — refactor(pwa): LandingPage → useQrJoin 抽出

## 1. Run 概要

| 項目            | 值                                                                                                                                         |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| Intent route    | CODE（純結構重構，scope: frontend/PWA）                                                                                                    |
| Dispatch 總數   | 4（coder 1、tester 1、reviewer 1、security-auditor 1）                                                                                     |
| 並行            | tester + reviewer 同批並行（另含 orchestrator 自跑 E2E typecheck）；0 重試、0 斷線                                                         |
| Fix Cycle 輪數  | 0（reviewer PASS：0 CRITICAL / 0 SUGGESTION）                                                                                              |
| Gate 輪數       | 需求確認 0、verify-before-test 0（皆由使用者簡報預先回答，autonomous session）、SUGGESTION 決策 0                                          |
| Security scan   | PASS（0 CRITICAL / 0 WARNING / 3 INFO 無攻擊路徑）                                                                                         |
| Diff stat       | 5 檔，+267 / −175（`LandingPage.tsx` 731→574 行；新增 `useQrJoin.ts` 191、`joinState.ts` 41、`apiHostGuard.ts` 17；測試檔僅 1 行同步註解） |
| Wall-clock 瓶頸 | tester dispatch 883s（reviewer 743s、coder 585s、security 465s）                                                                           |

## 2. 卡點與摩擦

- **U#**：無。Autonomous run，使用者輸入僅開場簡報與收尾 AskUserQuestion（提交方式、retro 決定）。
- **G0818_1636-1**：Glob `pwa/tests/e2e/**/*.ts` 回傳空結果，但檔案實際存在（Bash `ls` 找到 4 個 spec + tsconfig）——false negative 多花一輪確認 E2E 影響面。
- **G0818_1636-2**：`git add` 首次失敗（exit 128）：Bash cwd 跨呼叫殘留在 `pwa/`，`cd ..` 落點錯誤，改用絕對路徑重跑。

## 3. 改進提案

### Flow-structural（L#）

- **L0818_1636-1**：`target=.claude/skills/develop/references/code-cycle.md | sketch=比照既有 bug fast-path，新增 pure-refactor fast-path：當請求已釘死切法且驗收標準＝既有測試未修改全綠時，需求確認與 verify-before-test 兩個 gate 可折疊、於最終報告註記 | status=proposed`
- **L0818_1636-2**：`target=.claude/skills/develop/references/code-cycle.md | sketch=小型純搬移 diff 允許 Phase 6 security scan 與 Phase 4 初審並行 dispatch，條件：Fix Cycle 若有改檔則對改動檔補掃 | status=proposed`（本次序列化執行，事後看多等約 465s）

### 執行槓桿（E#，非 flow 修改）

- **E0818_1636-1**：worktree 內檔案存在性檢查優先用 Bash `ls`/`rg` 或 Glob 絕對路徑；相對 pattern 的空結果不可信（見 G-1）。
- **E0818_1636-2**：複合 git 指令一律以 `cd "<worktree 絕對路徑>" &&` 開頭，避免 Bash cwd 殘留（見 G-2）。
- **E0818_1636-3**：純搬移重構的 security-auditor prompt 可直接引用 reviewer 已機器驗證的逐字等價結論為已知事實，把稽核焦點限縮在新 export 面與不變量——本次 auditor 重做了一次完整機器比對（獨立驗證有其價值，但對零行為變更契約屬重複支出）。

## 4. Skill / Agent 增減建議

- 無增減。四角色循環對本任務合身；tester 與 reviewer 對純搬移案有部分重疊（皆做逐行比對）但產出互補（anti-drift／既有 flake 發現 vs React 語意／架構驗證），維持現狀。
- 附帶：tester 發現的範圍外既有 flake（`SettingsPage.test.tsx:158`）以 spawn_task 背景任務籤分流而非併入 PR，符合「不混捆不相關修改」，此模式值得沿用（無需寫入規則）。

## 5. 三軸 KPI

- speed: `bottleneck_seconds: 883`、`serialized_legs: 1`（security scan 可與初審並行）
- precision: `requirement_rounds: 0`、`critical_count: 0`、`suggestion_adopted/proposed: 0/0`、`rework_rt: 0`
- context: `dispatches: 4`、`self_reads: 1`（`LandingPage.tsx` 全讀，供撰寫 coder prompt）、`top_consumer: agent_dispatches（四次共約 41 萬 subagent tokens，tester/reviewer/security 對同一 diff 各自重跑驗證）`、可削減槓桿＝E0818_1636-3
- meta: `patch_proposed: 2`

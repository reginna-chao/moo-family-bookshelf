# Retro — fix/member-list-boundary-validation (0828_1130)

## 1. Run 概要

| 項目                              | 值                                                                              |
| --------------------------------- | ------------------------------------------------------------------------------- |
| Intent route                      | CODE(bug fast-path:root cause 已由使用者以 file:line 釘死)                      |
| Agent dispatches                  | 7(coder 3、tester 2、reviewer 1、security-auditor 1)                            |
| 其中因 API session limit 中途死亡 | 2(coder×1、tester×1,各需使用者手動「Try again」喚回)                            |
| Fix Cycle rounds                  | 1(S1+S2;prescription exception 自查取代 re-review)                              |
| Gate rounds                       | 需求確認 0(fast-path 併入)/ verify-before-test 0(處方即確認)/ SUGGESTION 決策 1 |
| CRITICAL / SUGGESTION             | 0 / 4(採 2、轉 follow-up 卡 2)                                                  |
| Security scan                     | PASS WITH WARNINGS(唯一 WARNING = 既有 S3 側門,已開卡)                          |
| Diff stat                         | 8 檔,+1973 / −6                                                                 |
| Wall-clock bottleneck             | tester 首次派遣 2802s(在最終 lint/typecheck 步驟被 session limit 砍掉)          |

## 2. 卡點與摩擦

- **G1 — 兩次 agent 中途死亡(API session limit)**:coder 首派 234s 後死亡,留下半成品 `memberValidation.ts`(依規重派全新 agent 覆寫);tester 首派 2802s 後死亡,但「測試已全數落地且 suite 綠」,只剩收尾 lint/typecheck——orchestrator 直接自跑驗證取代重派,省下一輪完整 tester。證據:兩段 partial output 與後續 `git status` 檢核。
- **G2 — 併行工具鏈的格式化雜訊**:coder 回報 `extension/public/manifest.json` 等檔被 Prettier 重排(非其所為);隨後 `.claude/**` 60+ 檔出現行尾/格式 M 雜訊。兩度以 `git restore` 清理,確認 diff 為空或純格式後還原。工作區若不即時清理,commit gate 會被污染。
- **G3 — main 在 run 期間前進 8 commits**:參考慣例分支(borrowValidation)在本 run 中途以 #144 合入 main,與本次變更重疊 3 檔;security-auditor 順帶偵測到,否則 push 時才會撞衝突。
- **U1 —(中途詢問,非糾正)**:「請問現在狀況是?」——進度呈現節奏 OK,無方向修正。

## 3. 改進提案

- **L0828_1130-1**:`target=.claude/skills/develop/references/code-cycle.md | sketch=agent 死亡的重派規則加一前置步:先檢查 worktree 現況,若交付物已完整落地(檔案齊、可自行驗證),orchestrator 直接接手驗證,只對缺口重派;全量重派僅限產出不可信時 | status=proposed`
- **L0828_1130-2**:`target=.claude/skills/develop/references/code-cycle.md | sketch=Phase 8 commit gate 前補一次 git fetch + log HEAD..origin/main;平行 session 高頻合併下,Phase 0 的快照到收尾時常已過期,rebase 需求應在報告中主動呈現 | status=proposed`
- **E1(執行槓桿)**:使用者 prompt 已含完整修法處方(file:line + 政策 + 測試結構)時,verify-before-test gate 併入最終報告並記錄偏離點,省一輪 round-trip——本 run 如此執行,無返工。
- **E2(執行槓桿)**:每個 agent 返回後立即 `git status --short` 對帳,非本任務檔案的變動當場查證並還原,不留到 commit gate。

## 4. Skill / Agent 增減建議

- 無新增/移除。tester 在死亡前已自行 stage 新檔並跑完 suite,是 G1 能低成本恢復的主因——維持「tester 邊做邊驗」的現行寫法即可。

## 5. 三軸 KPI

```
speed.bottleneck_seconds: 2802
speed.serialized_legs: 0
precision.requirement_rounds: 0
precision.critical_count: 0
precision.suggestion_adopted/proposed: 2/4
precision.rework_rt: 0
context.dispatches: 7
context.self_reads: 15
context.top_consumer: subagent dispatches(7 次,約 90 萬 subagent tokens)
context.lever: agent 死亡後先盤點磁碟現況再決定重派範圍(見 L0828_1130-1)
meta.patch_proposed: 2
```

# Run Retrospective — 0828_1822（feat/onboarding-endpoint-note）

## 1. Run 概要

| 項目             | 值                                                                                                                             |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| Intent route     | CODE（frontend：extension + pwa + shared）                                                                                     |
| Agent 派遣總數   | 10（Explore 1、coder 3、tester 3、reviewer 2、security-auditor 1）                                                             |
| 派遣折損         | 2（coder×1 API session limit 中斷後重派；tester×1 使用者中斷後重派，重派者驗證並保留前者已落地的部分成果）                     |
| Fix Cycle rounds | 1（reviewer 首輪即 PASS 零建議；唯一修復輪由 security-auditor INFO #1 觸發、使用者核准）                                       |
| Gate rounds      | 需求確認 1（AskUserQuestion：方案 A/B/C + 範圍）；verify-before-test 0（fast-path 折疊）；SUGGESTION 決策 1（INFO #1 修/不修） |
| Diff stat        | 12 files changed, +722 / −74                                                                                                   |
| Wall-clock 瓶頸  | tester（同址抑制測試）1294s；次高 reviewer 全審 946s                                                                           |

## 2. 卡點與摩擦

- **U1**（verbatim:「A：僅自訂時顯示」）：TL 建議 B（永遠顯示）被否決,使用者選擇沿用 repo 既有揭露 invariant（官方端點沉默）。教訓:repo 內已成文的設計 invariant 是使用者偏好的強訊號,TL 建議應優先與其對齊而非另立新界線。
- **U2**（verbatim:「[Request interrupted by user for tool use]」→「繼續派遣 tester」）:tester 派遣被中斷一次後原樣續派。中斷者已部分完成兩個測試檔,重派的 tester 以「驗證後保留」正確處理,未重工。
- **G1**:工作樹兩度出現非本任務的 prettier 全域重排殘影（前次 52 檔、本輪 coder 回報 2 檔:`extension/public/manifest.json`、`extension/tests/e2e/fixtures/mock-readmoo.html`）,來源無法歸因,每次都需 `git restore` + commit 時明列路徑防洩入 PR。gh 的「52 uncommitted changes」警告是唯一攔截點。
- **G2**:兩個 agent 因 API session limit 陣亡（本輪 coder×1;同 session 稍早 security-auditor、reviewer 各×1）,均依「重派全新 agent、不沿用部分輸出」規則恢復,無資料損失,但各折損一次完整 dispatch 的 token。
- **G3**:coder 首輪回報中已標記「同址雙註記」UX 觀察,當輪未處理;同一問題由 security-auditor 以 INFO 再度浮出,才觸發 coder+tester+聚焦複審一整輪。若在首輪規格內即裁決,可省一輪(≈3 dispatches)。

## 3. 改進提案

### Flow-structural

- **L0828_1822-1** `target=.claude/agents/coder.md（tester.md 同段）| sketch=Forbid repo-wide formatters (pnpm format / prettier --write without an explicit file list); format only files you touched | status=proposed`
  依據:G1。殘影檔案與 agent 活動時間窗重合但無法歸因;封鎖全域格式化是最小防護。
- **L0828_1822-2** `target=.claude/skills/develop/references/code-cycle.md | sketch=Phase 8 commit gate: run git status first; stage explicit paths only; stray files must be inspected, not swept in | status=proposed`
  依據:G1。本輪僅靠 gh 警告偶然攔下;應成為 commit gate 的固定步驟。

### 執行槓桿（no flow gap）

- **E1**:coder 回報中的「順帶 UX/風險觀察」若落在本次 feature 內,應當場請使用者裁決或折入同輪修正,不留給 Phase 6 重新發現（G3,省一輪 dispatch）。
- **E2**:TL 建議前先檢索 repo 內同主題的既有 invariant/註解;與其一致的選項預設為推薦項（U1）。

## 4. Skill / Agent 增減建議

- 無增減。security-auditor 在純 UI 變更上仍抓到 reviewer 未列的同址重複路徑（sync-remnant prefill),證明 post-feature 掃描對顯示層變更同樣有價值;維持現制。

## 5. 三軸 KPI

```
speed.bottleneck_seconds: 1294
speed.serialized_legs: 0
precision.requirement_rounds: 1
precision.critical_count: 0
precision.suggestion_adopted: 1
precision.suggestion_proposed: 2
precision.rework_rt: 1
context.dispatches: 10
context.self_reads: 4
context.top_consumer: agent dispatches
context.cuttable_lever: fold coder-flagged in-feature observations into the same round (see E1)
meta.patch_proposed: 2
```

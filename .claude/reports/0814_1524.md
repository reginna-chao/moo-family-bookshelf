# Retro — 0814_1524（fix/confirm-family-endpoint-switch）

## 1. Run 概要

| 項目                  | 值                                                                                                 |
| --------------------- | -------------------------------------------------------------------------------------------------- |
| Intent route          | CODE（frontend 安全修復,源自前一個 run 的 security-auditor WARNING）                               |
| Dispatch 總數         | 17（coder ×6、tester ×6、reviewer ×4、Explore ×1;其中 4 個 agent 遭 API/session limit 中斷需重派） |
| Fix Cycle rounds      | 5（0 CRITICAL 全程;R1 8 項 SUGGESTION、R3/R4 各再生一批;最終 reviewer PASS）                       |
| Gate rounds           | 需求確認 1（4 個設計決策一次批次);verify-before-test 1（一次通過);SUGGESTION/範圍決策 4            |
| Diff stat             | 48 檔,+6497 / −240（shared 2 新檔、extension 12、pwa 6、tests 22、docs 6）                         |
| 測試數                | extension 1909 → 2211（+302）;pwa 665 → 783（+118）                                                |
| Wall-clock bottleneck | tester dispatch（Round 3 測試更新),2233 秒                                                         |

## 2. 卡點與摩擦

| #   | 描述                                                                                                                                                                                                                                                                                                                                    | 證據                                                                                                                                                   |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| G1  | **範圍三度擴大,每次都由安全稽核觸發。** 起始範圍是「加確認面板」;R3 稽核發現同步碼出口仍散布被拒端點(本次改動引入的分歧)+ userinfo/IDN spoof;R4 發現 PWA 有同一條攻擊路徑且零防護;R5 稽核把先前列為 INFO 的「join 失敗滯留端點」升級為 WARNING(附完整攻擊鏈)。每次擴大都需回頭問使用者,共耗掉 3 個決策 gate。                           | R3 WARNING #1「改動前沒有分歧——舊的靜默採納保證 familyEndpoint === getEndpoint()」;R5「Re-rated to WARNING because the attacker controls the failure」 |
| G2  | **4 個 agent 遭 session limit / 連線中斷,3 次落在 tester。** 其中 2 次留下不完整的測試編輯,接手的新 agent 必須先「審查前一次留下什麼、哪些正確」才能繼續。我在重派前先自行跑測試取得精確失敗清單(8 個 failure 具名),讓接手 agent 省去重新探索。                                                                                         | tester ×3 中斷(162k / 148k / 156k subagent tokens);第 3 次重派時我先跑 `vitest run` 並 grep 出 4+4 個具名失敗塞進 prompt                               |
| G3  | **並行派遣需明確宣告檔案所有權,否則 agent 會誤判樹狀態。** 兩次 coder+tester 並行時,coder 在報告中把 tester 的 in-flight 編輯誤認為自己的 diff;我在後續 prompt 加上「another agent is concurrently editing X — you own Y exclusively」後不再發生。                                                                                      | R4 coder:「the other files in git diff --stat are my earlier work plus the tester's concurrent edits — I did not touch those」                         |
| G4  | **一個 coder 對指令做了有理據的偏離,需我裁決。** 我要求「performJoin 在 ANY failure 自我回滾」,coder 改成把 attempt scope 交給 handleJoin,理由是驗證挑戰期間立刻還原會讓 getVerifyMethod 打到官方端點,自架家庭重連死在「無法載入驗證方式」。我接受並在下一輪 reviewer prompt 明確要求驗證此取捨,reviewer 逐條走過每個出口後確認 sound。 | R5 coder「與指令的一處刻意偏離(重要,請 reviewer 覆核)」;R5 reviewer「偏離設計的驗證:合理」                                                             |

U#:無——使用者全程無中途修正,7 個決策點皆採 TL 建議。

## 3. 改進提案

**Flow-structural:**

- L0814_1524-1 `target=.claude/skills/develop/references/code-cycle.md | sketch=Phase 6 安全掃描改為「功能完成前先跑一次 changed-mode 預掃」,把會擴大範圍的 WARNING 提前到 Phase 1 需求分析,而非在收尾前才逼出三輪範圍決策 | status=proposed`
- L0814_1524-2 `target=.claude/skills/develop/SKILL.md | sketch=§3 並行派遣段落補一行:並行 dispatch 時每個 agent prompt 必須明列「你獨佔哪些檔案 / 另一個 agent 正在改哪些」,否則 agent 會把他人 in-flight 編輯誤認為自己的 diff | status=proposed`
- L0814_1524-3 `target=.claude/skills/develop/SKILL.md | sketch=§3 的「agent 中斷需重派全新 agent」補上:重派前先由 orchestrator 自行執行驗證指令取得精確失敗清單並寫進 prompt,接手成本從「重新探索」降為「照清單修」 | status=proposed`

**執行槓桿(no flow gap):**

- E1:安全修復類任務,把「這次改動是否引入新的分歧」列為 reviewer prompt 的固定 focus 項。R3 WARNING #1 正是本次改動製造的(移除靜默採納後,同步碼來源與本機採用值脫鉤),不是既有問題。
- E2:coder 回報的「Open Questions」要逐條裁決,不要略過。本次 R4/R5 的 coder 各提 4–6 個問題,其中「架構文件未同步」「CHANGELOG 未更新」「onFamilyRemoved 是否比照處理」三項若略過就會漏交付。
- E3:reviewer 的 prescription 若涉及多檔互相牽動(本次 shared 抽取牽動 6 檔),交由單一 coder 而非拆並行,避免衝檔。

## 4. Skill / Agent 增減建議

無新增。本次驗證了現有配置的價值:security-auditor 連續三輪都找出 reviewer 沒看到的攻擊鏈(reviewer 判 PASS 的同一份程式碼,auditor 給出可執行的 exploit),兩個角色不可互相取代。Explore agent 在 Phase 1 一次掃出 8 個面向的完整地圖(含 Firefox 缺口、測試零覆蓋、文件過時三個我沒問的發現),是本次少數一次到位的 dispatch。

## 5. 三軸 KPI

- speed: bottleneck_seconds: 2233; serialized_legs: 2（R3 reviewer 與 security-auditor 已並行;R4/R5 的 coder→tester 因檔案相依無法並行）
- precision: requirement_rounds: 1; critical_count: 0; suggestion_adopted/proposed: 12/16; rework_rt: 5（五輪 Fix Cycle,其中 3 輪由安全稽核觸發而非審查缺陷）
- context: dispatches: 17; self_reads: 14; top_consumer: tester dispatches（6 次共約 1.05M subagent tokens）; lever: L0814_1524-1——安全預掃前移可消除 3 輪範圍擴大所帶動的重複 tester 往返
- meta: patch_proposed: 3

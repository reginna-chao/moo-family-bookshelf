# Retro 0811_2202 — 補 updateEndpointRoute 測試（test/family-endpoint-coverage）

任務：替 worker 覆蓋率最低的 `PUT /api/family/:id/endpoint` handler（`worker/src/routes/family.ts:894-1025`）補測試，test-only、不改 production 行為；發現缺陷回報不修。

## 1. Run 概要

| 項目           | 數值                                                                                                                  |
| -------------- | --------------------------------------------------------------------------------------------------------------------- |
| Intent route   | CODE（test-only）                                                                                                     |
| Agent 派發     | 4（tester 1 + tester resume 1 / reviewer 1 / security-auditor 1；coder 0——無 production 變更）                        |
| 並行派發       | 1 組（reviewer ∥ security-auditor）                                                                                   |
| Fix Cycle 輪數 | 1（CRITICAL 0；SUGGESTION 2 → 採納 2，處方例外自核 diff，未派 re-review）                                             |
| Gate 輪數      | 需求確認 0 / verify-before-test 0（test-only 不適用）/ SUGGESTION 決策 1（單次 AskUserQuestion 併答 4 題）            |
| Diff           | 1 檔新增，+761 行（87+1 個測試）                                                                                      |
| 最慢派發       | tester 984s（reviewer 484s、auditor 558s、tester resume 126s）                                                        |
| 驗收           | typecheck / lint / test 全綠；758 → **846** 測試；`family.ts` 行覆蓋 **79.12% → 93.29%**；worker 整體 92.22% → 95.39% |
| 特殊條件       | 主 checkout 被另一 session（publicShelf 下沉）佔用 → 以 EnterWorktree 隔離開工，全程未觸碰主 checkout                 |

Production 缺陷（回報未修，已開背景任務 chip `task_3c708653`，優先序 B1>B2>B3）：B1 原始型別 JSON body → 500（`family.ts:923`、`:642`）；B2 IPv6 字面量繞過私網阻擋（`family.ts:976-995`）；B3 127.0.0.0/8 除 `.1` 外放行。auditor 分級全部 INFO（唯一行為者是 owner，其本就能指向任意公開 https 主機）；B3 留有產品決策點（全擋或視同 localhost）。

## 2. 卡點與摩擦

- **G1 — 主 checkout 被並行 session 佔用。** 開工時發現 working dir 在別的 session 的分支上（`refactor/extract-public-shelf-service`），不能切分支。以 EnterWorktree 建立隔離 worktree 解決，`pnpm install` 6.9s 即可用。非失誤，但這是本專案首次雙 session 並行，模式值得記錄：先 `git worktree list` + `git branch --show-current` 判定佔用，再決定隔離。
- **G2 — `git fetch origin` 一次性 SSH 失敗**（"correct access rights"），origin/main ref 恰好已新鮮（另一 session 剛 fetch 過）故未受阻；push 前需重試確認。未查根因，若復發再深究。
- U#：無（任務指令一次到位；4 個決策一次 AskUserQuestion 併答，零往返）。

## 3. 改進提案

- **L0811_2202-1**：`target=.claude/agents/tester.md | sketch=固化「缺陷不封規格」協定——測試發現 production「應擋未擋」時，禁止寫下把該行為釘成預期的斷言，一律列入報告 Production Bugs Found 交使用者決策；本 run 靠 orchestrator 在 prompt 內手寫此規則，應下沉為 tester 常規 | status=proposed`
- **不提升（G1）**：worktree 隔離已寫在 code-cycle Phase 0（"or create a new worktree"）與其 step 6 的 agent prompt 規範，本 run 照既有規則執行即可，無需新增。

執行槓桿（E#）：

- **E1**：SUGGESTION 修法屬處方（reviewer 給逐字 diff、≤5 行）時，用 SendMessage 恢復原 tester 套用（126s、context 全在）遠優於重派新 agent；「處方例外」自核 diff 免 re-review 也如規則預期運作。
- **E2**：多個獨立決策（SUGGESTION 採納、缺陷處置、retro、提交方式）併進單次 AskUserQuestion——上次 retro 的 G3 教訓本次落地，零額外往返。
- **E3**：boundary-test 類任務的 tester prompt 附上「ground truth 行為地圖 + 要求逐項自行驗證」寫法有效：87 個測試一次全對、reviewer 逐列複核零錯誤。

## 4. Skill / Agent 增減建議

- 無增減。tester(opus) 在安全邊界測試任務上的產出品質（含主動發現三個 production 缺陷、正確地不封規格）證明 opus override 用在這類任務是對的；reviewer 逐列用 WHATWG URL 實測斷言、auditor 用反證鏈給分級，兩者並行的組合維持。

## 5. 三軸 KPI

```
speed:
  bottleneck_seconds: 984
  serialized_legs: 0
precision:
  requirement_rounds: 0
  critical_count: 0
  suggestion_adopted/proposed: 2/2
  rework_rt: 0
context:
  dispatches: 4
  self_reads: 3
  top_consumer: tester 派發（170k subagent tokens）
  lever: 處方修法一律走 SendMessage resume（見 E1）
meta:
  patch_proposed: 1
```

# Retro 0813_2255 — verify.ts per-userId 寫入限流(fix/verify-write-ratelimit)

## 1. Run 概要

| 項目                | 值                                                                                                     |
| ------------------- | ------------------------------------------------------------------------------------------------------ |
| Intent route        | CODE(backend,bug fast-path)                                                                            |
| Agent dispatch 總數 | 6(coder 2、tester 2、reviewer 1、security-auditor 2 其中 1 次中斷)+ SendMessage 續派 2(coder 文件補列) |
| Fix Cycle rounds    | 1(0 CRITICAL;SUGGESTION 6 提 4 採)                                                                     |
| Gate rounds         | 需求確認 0(fast-path 併入)/ verify-before-test 0 / SUGGESTION 決策 0 中途、1 次期末追認                |
| Diff stat           | 6 files,+791 / −30                                                                                     |
| Wall-clock 瓶頸     | reviewer dispatch 675s                                                                                 |

範圍註記:稽核原列 3 個 handler,Phase 1 自讀 `worker/src/routes/verify.ts` 發現第 4 個(`POST /:id/verify/otp`,verify.ts:200)漏列,一併涵蓋。`family.ts` 五個 handler 依使用者指示遞延(`fix/join-ratelimit-owner-dos` in-flight)。

## 2. 卡點與摩擦

- **U1**(使用者輸入,verbatim):「Try again」— security-auditor 首次派遣因 API session 用量上限中斷後,使用者要求重試。
- **G1**:security-auditor 首派中斷於任何發現產出之前,依規則全新重派 → 探索工作全額重做(重派耗 653s / 143k tokens)。
- **G2**:外部稽核清單漏列 otp handler;靠 orchestrator 全檔自讀補抓。證據:verify.ts:200。
- **G3**:reviewer 發現 diff 外的文件漂移(architecture.md 過時「公開」標註、缺 qr-token 端點列與 `qr:{token}` KV 列),coder 以 2 次 SendMessage 逐項追加 — 若首輪處方即含「同步所有觸及端點的表格」授權,可壓成 1 次。

## 3. 改進提案

- **L0813_2255-1**:`target=.claude/skills/develop/references/code-cycle.md | sketch=Phase 1 bug-intake 補一行:外部稽核/issue 提供的清單(handler、檔案)寫入 coder prompt 前必須對 source 重新枚舉,清單視為線索非事實 | status=proposed`
- **L0813_2255-2**:`target=.claude/skills/develop/references/code-cycle.md | sketch=Phase 4 fix-round coder 處方涉及文件同步時,一次授權「同步所有觸及端點/key 的表格列」的有界掃描,取代逐列追認 | status=proposed`
- **L0813_2255-3**:`target=.claude/skills/develop/references/code-cycle.md | sketch=§4.3 補 autonomous session 規則:無法中途詢問時採 TL 建議(修 🟢、跳 🔴),決策全數記錄於期末報告供追認 — 本 run 為臨場即興,應成文 | status=proposed`
- **E1**(執行槓桿):小 diff 的 security-auditor 緊接同 diff 的 reviewer 之後時,把 reviewer 的不變式追溯結論嵌入 auditor prompt,砍掉重複的探索讀檔(本 run auditor 32 次 tool use 大半重走 reviewer 走過的路)。
- **E2**(執行槓桿):併行 fix-round(coder 改 docs、tester 改測試檔)前先聲明檔案互斥清單於雙方 prompt — 本 run 已做且零衝突,維持此習慣。

## 4. Skill / Agent 增減建議

- 無增減。唯一觀察:reviewer 與 security-auditor 在 <100 行 diff 上重疊度高,E1 的 prompt 層手法即可緩解,不值得動 agent 結構。

## 5. 三軸 KPI

- speed: `bottleneck_seconds: 675`,`serialized_legs: 0`
- precision: `requirement_rounds: 0`,`critical_count: 0`,`suggestion_adopted/proposed: 4/6`,`rework_rt: 0`(另有 2 次文件補列 SendMessage,屬範圍擴充非返工,見 G3)
- context: `dispatches: 6+2`,`self_reads: 2 全檔 + 4 節錄`,`top_consumer: subagent dispatches(單筆最大 tester 195k)`,可砍槓桿=E1
- meta: `patch_proposed: 3`

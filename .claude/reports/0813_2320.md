# Retro — 0813_2320（auth-seed helper follow-ups）

## 1. Run 概要

| 項目             | 值                                                                                                                                             |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| Intent route     | CODE（backend,純測試變更)                                                                                                                      |
| Dispatch 總數    | 3(tester ×1、reviewer ×1、security-auditor ×1;coder ×0)                                                                                        |
| Fix Cycle rounds | 1(CRITICAL 0、SUGGESTION 3 提出 / 0 採納)                                                                                                      |
| Gate rounds      | 需求確認 0(autonomous、任務全預先指定)/ verify-before-test 0(無 production code)/ SUGGESTION 決策 1(與 commit、retro 併成一次 AskUserQuestion) |
| Diff stat        | 3 檔:helpers/auth.ts +78(逐字複製)、userDelete.test.ts 3+/10−、authRefresh.test.ts 3+/5−                                                       |
| Wall-clock 瓶頸  | security-auditor 499s(reviewer 416s、tester 236s)                                                                                              |

## 2. 卡點與摩擦

- **G1 — 任務前提與實際 git 狀態落差**:任務描述 helper「introduced on branch `test/extract-auth-seed-helper`」,但開工當下該分支 tip == `origin/main`,helper 只存在於另一個 worktree 的**未提交**工作區。若直接照「branch fresh from origin/main」開工會編譯失敗。處置:`git cat-file -e` 逐層查證 → 逐字複製 + SHA-1 驗證;run 中途對方 session 補上 commit(`2b33ee2`),reviewer 以 blob hash 覆核 byte-identical。損失約 4 次額外 git 查證呼叫,無 rework。
- **G2 — 任務隱含的決策映射前提有誤**:item 2 的判斷規則(null-record 分支已被 unit 覆蓋 ⇒ 改名)成立,但「改名」會使該測試淪為 `authRefresh.test.ts:63` 的完全重複(同 fixture、斷言子集),且 route 層 mint 分支零覆蓋——此事實僅在通讀整檔後浮現。處置:改採任務提供的另一選項(真正不 seed),於報告中完整交代偏離理由。無 rework。

## 3. 改進提案

- **L0813_2320-1**(flow-structural):`target=.claude/skills/develop/references/code-cycle.md | sketch=Phase 0 加一步「任務引用的外部產物(分支/檔案/helper)先以 git cat-file/branch 驗證實際位置與 commit 狀態,再選 base」 | status=proposed`
- **L0813_2320-2**(flow-structural):`target=.claude/skills/develop/references/code-cycle.md | sketch=Phase 6 對 tests/** -only diff 允許降階掃描(單一 test-integrity 維度),本次 full-blast auditor 花 499s/95k tokens 換 PASS+3 INFO | status=proposed`
- **E1**(執行槓桿):對「已存在某處」的前提,規劃前先花一次 `git cat-file -e` / `git status` 查證——本次靠這步避開了無法編譯的 base。
- **E2**(執行槓桿):即使任務給了精確行號,仍通讀目標檔全文——item 2 的重複測試發現(G2)只可能來自通讀。
- **E3**(執行槓桿):小 diff 時 reviewer + security-auditor 同訊息並行,本次 serialized legs = 0,維持此習慣。

## 4. Skill / Agent 增減建議

- 無增減。tester / reviewer / security-auditor 各盡其職;auditor 對純測試 diff 偏重(見 L-2),屬流程調參而非 agent 存廢問題。

## 5. 三軸 KPI

```
speed.bottleneck_seconds: 499
speed.serialized_legs: 0
precision.requirement_rounds: 0
precision.critical_count: 0
precision.suggestion_adopted/proposed: 0/3
precision.rework_rt: 0
context.dispatches: 3
context.self_reads: 7
context.top_consumer: subagent dispatches(約 256k subagent tokens)
context.lever: tests-only diff 的 Phase 6 降階(見 L0813_2320-2)
meta.patch_proposed: 2
```

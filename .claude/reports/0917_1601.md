# Run 回顧 — 0917_1601

「CI/CD 三輪檢測是否該拆成三個檔案」諮詢 → 查證後轉為 CODE：修 `ci-success` 真空放行洞 + tripwire；ruleset 必要檢查改為 `CI Success`

## 1. Run 概要

| 項目                  | 值                                                                                                          |
| --------------------- | ----------------------------------------------------------------------------------------------------------- |
| Intent route          | 起點是諮詢（非 CODE / DESIGN），查證發現 ruleset 與 workflow 脫鉤後由使用者選「現在修」轉入 CODE full cycle |
| Mode                  | full cycle；fix mode 不適用（`.github/workflows/**` 在 condition 4 清單）                                   |
| Dispatch 數           | 6（coder 1、tester 2、reviewer 2、security-auditor 1）                                                      |
| 平行 vs 序列          | security-auditor 背景執行、與 S1 決策 + tester 2 平行；其餘序列                                             |
| Fix Cycle 輪數        | 2（Round 1：SUGGESTIONS S1 / S-2，0 CRITICAL；Round 2：聚焦複審 PASS）                                      |
| Gate 輪數             | 需求確認 2（結構 + ruleset；修不修洞）、verify-before-test 0（折疊）、SUGGESTION 決策 1、commit gate 1      |
| Security scan         | PASS WITH WARNINGS（0 CRITICAL、2 WARNING → #195、#196）                                                    |
| 範圍外 P1 issue       | 3（#194 reviewer S-2；#195、#196 security WARNING）                                                         |
| Diff stat             | 2 files changed, 220 insertions(+), 2 deletions(-)；`CHANGELOG.md` 依 Rule 2 未動                           |
| 對外生效動作          | ruleset `main-protection` 必要檢查 3 個 job context → `CI Success`（有修改前備份）；3 張 issue              |
| Wall-clock bottleneck | reviewer Round 1，512 秒（自建 7 變體 mutation harness）；security-auditor 441 秒但在背景                   |

## 2. 卡點與摩擦

**U（使用者修正）**：無。四次 AskUserQuestion 皆選 TL 建議選項，無逐字修正。

**G1（我自己的誤判，被 coder 糾正）**：向使用者提案時我寫「這條寫不出會失敗的自動化檢查，GitHub
Actions 的 `if:` 求值沒辦法在 repo 內單元測試」。coder 回報 Open Question 指出 grep-shaped tripwire
可寫（`worker/tests/unit/kvAccessBoundary.test.ts`、`cleanKvScript.test.ts` 已是先例），修復前紅、
修復後綠。`change-triage.md` 第 3 項是 gate，我在 gate 上答錯了方向。誠實更正後由 tester 落地，
mutation check 證實只有 `changes` 那一條轉紅。**「能不能寫失敗檢查」的判斷要先掃 `worker/tests/unit/`
的既有 tripwire 形狀，再下結論。**

**G2（scope 無法標記）**：`coder.md` / `tester.md` / `reviewer.md` 的 `scope` 只接受 `frontend` /
`backend`。本次 production 檔是 `.github/workflows/cicd.yml`，兩者都不適用；我在三個 prompt 各自手寫
「不要讀 frontend.md / backend.md、不要跑 pnpm typecheck、改用 prettier --check」。tester / reviewer
則借用 `backend`（測試檔落在 `worker/tests/`），可行但要在 prompt 內額外聲明「backend.md 的 Worker
runtime 慣例不適用」。每個 dispatch 多花一段免責文字，且下次不一定記得。

**G3（verify-before-test gate 無對應形式）**：CI YAML 改動沒有可點、可 curl 的東西，本機觀察不到
GitHub 的 `if:` 求值。`code-cycle.md` 的折疊條款只寫給「user unreachable」；使用者明明在線，我仍折疊
並當場說明理由（改動由使用者逐字指定、diff 逐字吻合、驗收可由 mutation-checked tripwire 機器化）。
結果正確，但這是我自己造的例外，不是流程給的。`Pure-refactor fast path` 已有「驗收 = 凍結套件」的
N/A 先例，這類「驗收 = 帶 mutation check 的 tripwire」缺對應條款。

**G4（Phase 6 area 計數把測試檔算成一個 area）**：`cicd.yml` → `config`，
`worker/tests/unit/ciSuccessGate.test.ts` → `worker`，2 areas → `full`。auditor 自陳 Dimension 4 / 5 / 6 / 8
全部 N/A（diff 無 surface），兩項 WARNING 都來自 Dimension 1 / 7，`config` 行自帶的 `secrets` scope
即可覆蓋。`full` 相對 `secrets` + `code` 多買到 0 個 finding，花 441 秒、111k subagent tokens。
fix mode 的 40 行量測已明文排除三個 test tree，area 計數卻沒有，兩處規則對「測試檔算不算」答案不一致。

**G5（處方偏離觸發聚焦複審，規則正確運作）**：tester 把 reviewer 處方的 `^\s*` 收緊成 `^ {4}`，
理由是 `GATE_BLOCK` 涵蓋 `steps:`，寬鬆版會被 step 層 `if:` 空轉滿足。依 §4「偏離 → 聚焦複審」
派了 reviewer Round 2（187 秒），裁決 PASS 且確認偏離「嚴格較佳」。不是摩擦，記錄為規則有效的
證據：偏離不一定是退步，但一定要有人看。

**G6（環境小坑，1 次重試）**：Bash 的 `/tmp` 對應 Windows TEMP，但 node 把 `/tmp/x` 解析成
`D:\tmp\x` → ENOENT。改用 `$TMP` 環境變數後正常。`memory/project_bash_sandbox_quirks.md` 尚未記這條。

**G7（issue body 打錯分支名，事後 `gh issue edit` 補正）**：#195 出處段寫成 `fix/ci-server-gate`。
用 heredoc 手打長 body 時，分支名這種可查的值應從 `git rev-parse --abbrev-ref HEAD` 帶入，不手打。

## 3. 改進提案

### Flow-structural

- **L0917_1601-1** — `target=.claude/agents/coder.md, tester.md, reviewer.md` |
  `sketch=scope 增加第三值 config（.github/workflows/**、wrangler.toml、package.json 等 condition 4 的
設定檔）：不讀 frontend.md / backend.md，verify = prettier --check + 該檔自有的靜態檢查；測試落
worker/tests/unit/ 時 tester / reviewer 沿用 backend 的測試規則但明文豁免 Worker runtime 慣例` |
  `status=proposed`
  依據 G2。**先查 code-cycle.md 是否已有「非 FE/BE 檔案怎麼標 scope」的段落**（cut before add）；
  若無，三個 agent 檔各補一行比在 code-cycle.md 加一節便宜。

- **L0917_1601-2** — `target=.claude/skills/develop/references/code-cycle.md` |
  `sketch=Phase 3 的 Pure-refactor fast path 旁邊補一條「Tripwire-acceptance fast path」：當改動的
驗收條件是一支帶 mutation check 的 guard test（修復前紅、修復後綠）且本機無可觀察行為時，
verify-before-test gate 宣告 N/A，理由當場寫出` | `status=proposed`
  依據 G3。與既有 fast path 同形，只是 oracle 從「凍結套件不變」換成「guard 的紅綠翻轉」。

- **L0917_1601-3** — `target=.claude/skills/develop/references/code-cycle.md` |
  `sketch=Phase 6 area 計數排除 extension/tests、pwa/tests、worker/tests（與 fix mode 條件 3 的排除
清單對齊）；測試檔仍參與 scope 行的 union（code），只是不觸發 multiple areas → full` |
  `status=proposed`
  依據 G4。兩處規則對測試檔的處理要一致；若 `/distill` 認為測試檔該算 area，則反向修 fix mode 的
  排除清單，總之二選一。

- **L0917_1601-4** — `不提升：G1 是判斷失誤，不是流程缺口`。`change-triage.md` 第 3 項已經是
  gate；缺的是執行時去看先例，見 E1。

### 執行槓桿（no flow gap）

- **E1**：宣告「寫不出失敗檢查」之前，先 `ls worker/tests/unit/ | grep -iE 'boundary|parity|script|gate'`
  看 grep-shaped tripwire 先例；能讀檔就能斷言，CI 設定檔不例外。
- **E2**：使用者質疑 CI/CD 結構時，第一步同時查 `gh api repos/:o/:r/rulesets` 與
  `branches/main/protection`，不只讀 yml——本次真正的缺陷（必要檢查指向錯誤 context、E2E 不在
  gate 內）在 GitHub 設定裡，yml 本身看不出來。
- **E3**：production 凍結後 tester 與 reviewer 可平行（§3 步驟 4 明文允許），本次序列跑掉約 258 秒。
- **E4**：node 讀寫暫存檔用 `$TMP/…`，不用 `/tmp/…`（G6）；補進 `project_bash_sandbox_quirks.md`。
- **E5**：issue / PR body 裡的分支名、commit sha 由 git 指令帶入變數，不手打（G7）。

## 4. Skill / Agent 增減建議

- **coder / tester / reviewer**：加 `config` scope（L1）。這是本 run 最實際的摩擦，三個 prompt 都在
  重複同一段免責文字。
- **security-auditor**：不動。`mode: changed` + 自陳 N/A 維度的做法很誠實，問題在 orchestrator 端的
  area 計數（L3），不在 auditor。
- **/develop §0 intent fork**：本次起點是純諮詢，§0 三個 route 都不吻合；我先做了讀檔與 gh 查證再
  用 AskUserQuestion 轉 CODE，這是對的路徑，但 SKILL.md 沒寫「諮詢 → 查證 → 用選項問要不要轉 CODE」
  這條。不提升（一句話就能做，寫進去反而讓 §0 變長）。

## 5. 三軸 KPI

- speed: bottleneck_seconds=512 (reviewer round 1) ; serialized_legs=1 (tester 1 → reviewer 1 可平行)
- precision: requirement_rounds=2 ; critical_count=0 ; suggestion_adopted/proposed=1/2 ; rework_rt=1 (focused re-review after prescription deviation)
- context: dispatches=6 ; self_reads=2 (cicd.yml 全讀 1 + ci-success 段 1；process 檔另讀 6) ; top_consumer=reviewer round 1 (141k subagent tokens) ; lever=tester 與 reviewer 平行派工 (E3)
- meta: patch_proposed=3

# Run 回顧 — 0917_1138

`/bump-ver 1.7.1` 發版（版本號 + CHANGELOG 升版 + 雙語 release notes，無 production code）

## 1. Run 概要

| 項目                  | 值                                                                                             |
| --------------------- | ---------------------------------------------------------------------------------------------- |
| Intent route          | 非 /develop：`/bump-ver` 自有流程（明文跳過 Fix Cycle）                                        |
| Dispatch 數           | 0（coder 0、tester 0、reviewer 0、security-auditor 0）                                         |
| 平行 vs 序列          | 全序列，無可平行化的腿                                                                         |
| Fix Cycle 輪數        | 不適用；等效修正輪 1（PR 審查回饋 2 項 → 1 個 commit）                                         |
| Gate 輪數             | plan gate 1（Step 4 計畫，使用者回「kv_ops 那條刪掉」）、AskUserQuestion 1（NITPICK 處理方式） |
| 外部審查              | 2 份：CI Claude review（WARNING 1 + NITPICK 1，皆採納）、本機 Codex（P2 ×1，退回）             |
| Diff stat             | 7 files changed, 74 insertions(+), 7 deletions(-)；commit 2                                    |
| Wall-clock bottleneck | `pnpm typecheck` 全 workspace，約 60 秒（無 agent dispatch，無更慢的腿）                       |

## 2. 卡點與摩擦

**U1（使用者決策，逐字）**：「kv_ops 那條刪掉，其他照這樣做」
計畫階段我把 #173 的 `kv_ops` log 放進「開發者體驗」並自陳「你若覺得沒必要我就刪掉」。使用者刪掉了。
`user-facing-copy.md` Rule 2 本來就傾向刪除讀者觀察不到的變更，而「只有自架者在 log 看得到」離
CHANGELOG 讀者太遠——**把判斷丟給使用者，其實是規則已經給了答案而我沒有執行**。

**U2（使用者決策，逐字）**：NITPICK 選了「另開一條 bullet」
review bot 建議把「待處理申請會被取消」併進原句，我提了三個選項並指出併入會讓主 bullet 變三句、
踩 Rule 8 深度上限。使用者選了獨立 bullet。這次沒有摩擦，記下來是因為它確認了
**Rule 8 與 Rule 6 在「補一個事實」時的正確解法是拆 bullet，不是延長句子**。

**G1（適用範圍寫太寬，兩次）**：CI review 的 WARNING 指出「建立或加入家庭的畫面會標出⋯⋯」
把只在擴充功能落地的功能寫成兩個平台都有——`pwa/src/components/SyncCodeHostNote.tsx:17-18`
的註解白紙黑字寫著 PWA 沒有對應畫面。我在 Step 2 把 #156 判為「新草稿」時只讀了 commit message
（「The extension onboarding container now renders⋯⋯」其實已經寫明只有 extension），沒有回頭
確認**這個變更落在幾個 surface 上**。`user-facing-copy.md` Rule 5 要求「誰受影響」必須保留，
Step 4 的 checklist 卻沒有任何一項會問這件事。

**G2（同一類的第二個實例）**：本機 Codex 反饋指出 v1.7.0 的
「**請更新擴充功能／PWA**：擴充功能會自動更新⋯⋯」對 README 折疊區裡的手動安裝者不成立。
該行位於已 tag 的 `## v1.7.0` 條目、且是 `7c3dce1`（#192）寫的，依凍結規則不能改，因此**退回**——
但**觀察本身成立**，而且它和 G1 是同一類錯誤：user-facing bullet 漏寫適用範圍。
上一份報告 `0916_1722.md` 的 L0916_1722-2 已經提過近親問題（要求讀者行動的 bullet 必須寫出
「怎麼做」）。**「怎麼做」缺一半、「對誰成立」缺另一半，兩份報告各撞到一次。**

**G3（外部審查基準過期，無損但花了時間查證）**：Codex 報告的比較基準是
`6ed83cc`，落後 `origin/main`（`7c3dce1`）三個 commit，於是 #192 重寫的 v1.7.0 文案在它的 diff 裡
看起來像本 PR 新增的。查證成本：3 次 git 指令（`git log 6ed83cc..origin/main`、`git log -L`、
`awk` 定位行號所屬的 `## ` 標題）。**外部審查回報一個「本 PR 新增」的問題時，第一步永遠是對基準
與行號歸屬**，否則會去修一個已經凍結、而且不是自己寫的條目。

## 3. 改進提案

### Flow-structural

- **L0917_1138-1** — `target=.claude/rules/user-facing-copy.md` |
  `sketch=Rule 5 補一條：bullet 描述的功能落在哪些 surface（擴充功能／PWA／自架 Worker）必須寫出來，
落在單一 surface 時句首指名該 surface` | `status=proposed`
  依據 G1 + G2。**先檢查能否併進 Rule 5 既有的「誰是豁免的」那句**（cut before add）——
  Rule 5 已經要求保留「使用者架設官方伺服器不受影響」這類條件，surface 範圍是同一件事的另一面，
  很可能只需要在該句擴一個例子，不必新增條目。與上一份報告的 L0916_1722-2 同源，`/distill` 時
  建議合併評估。

- **L0917_1138-2** — `target=.claude/skills/bump-ver/SKILL.md`（Step 2 分類 / Step 4 checklist）|
  `sketch=分類一個 commit 為「新草稿」時，必須確認它落在幾個 surface，再寫 bullet` |
  `status=proposed`
  依據 G1。本 run 是靠 CI review 才抓到，`/bump-ver` 自己的流程從頭到尾沒有這一關。
  實作成本低：Step 2 已經在讀 commit message，多一句「記下 surface」即可。

- **不提升**：U1 不另開提案。Rule 2 已經涵蓋「讀者觀察不到就刪」，缺的是執行而非規則；
  列為 E# 槓桿。

### 執行槓桿（無流程缺口）

- **E0917_1138-1** — 計畫階段對 Rule 2 邊緣的 bullet，**直接依規則刪掉並在計畫中一行說明
  「已依 Rule 2 刪除，理由 X」**，而不是保留它再問使用者要不要。使用者要留自然會說。
  本 run 的 `kv_ops` 走了「保留 + 問」，多花一個往返（U1）。

- **E0917_1138-2** — 收到任何外部審查（CI bot 或本機工具）的 finding，動筆前先跑
  「基準 + 歸屬」兩步：`git log <他的基準>..origin/main` 確認基準是否過期，
  `awk 'NR<=N && /^## /{last=$0} END{print last}' <file>` 確認行號落在哪個版本條目。
  兩條指令，防的是修改已凍結條目這種不可逆的錯（G3）。

- **E0917_1138-3** — 補事實到既有 bullet 時，預設拆成新 bullet 而不是延長句子
  （Rule 6 + Rule 8 的交集）。本 run 由使用者確認過一次（U2），可以直接當預設值，不必再問。

## 4. Skill / Agent 增減建議

- **不新增任何 skill / agent。** 本 run 0 dispatch，`/bump-ver` 的流程本身是夠的。
- **`/bump-ver` 可以瘦身一處**：Step 3 的 speak-human-tw 自動 pass 對「提升既有未釋出 bullet」
  這一半是空轉——那些 bullet 在原本的 run 就跑過一次去 AI 味了。本 run 的 pass 只在提升的文案裡
  找到 1 處（「一律」），而它是 #189 改寫時留下的。可考慮把 pass 的範圍縮到「新草稿的 bullet +
  release notes 英文半邊」，省一次全文掃描。
- **使用者角度的最大改善**：本 run 真正的把關是 CI 上的 Claude review，不是 `/bump-ver` 自己的
  checklist。若 L0917_1138-2 採納，這類問題會提早一個 gate 被抓到，PR 上就少一輪往返。

## 5. 三軸 KPI

```
speed: bottleneck_seconds=60 (pnpm typecheck, full workspace) | serialized_legs=0
precision: requirement_rounds=1 | critical_count=0 | suggestion_adopted/proposed=2/3 | rework_rt=1
context: dispatches=0 | self_reads=14 | top_consumer=git 歷史查證（commit body + CHANGELOG diff，約 6 次） | cuttable=Step 2 的 commit↔bullet 對照改用單次 `git show <hash> -- CHANGELOG.md` 批次取得，取代逐一 `git log -1 --format=%B`
meta: patch_proposed=2
```

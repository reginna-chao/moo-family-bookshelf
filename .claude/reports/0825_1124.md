# Run Retrospective — 0825_1124

## 1. Run 概要

| 項目                | 值                                                                                                                    |
| ------------------- | --------------------------------------------------------------------------------------------------------------------- |
| Intent route        | CODE（follow-up fix：stale reset-timer supersede；由前次 run 的 chip 轉為本 session 接手）                            |
| Agent dispatch 總數 | 12（coder 2、tester 7、reviewer 2、security-auditor 1）                                                               |
| dispatch 損耗       | tester 5 次被 API 529 於啟動即殺（見 G1）、reviewer 1 次早期 stall（見 G2）；實際完成工作的 dispatch 為 6             |
| Fix Cycle 輪數      | 2（R1：3 項 SUGGESTION 全採納；R2：處方例外自查收斂，未再派 re-review）                                               |
| Gate 輪數           | 需求確認 0（bug fast-path 併入 verify-before-test）、verify-before-test 1（一次過）、SUGGESTION 決策 1、commit gate 1 |
| Diff stat           | 8 files, +248 / −32（production 5、tests 3 檔 4 例，全數 mutation-checked）                                           |
| Wall-clock 瓶頸     | 529 風暴阻斷 tester 派工約 75 分鐘（退避階梯 3→10→30→45 分）；最慢成功 dispatch 為 S1+S3 tester ≈ 724s                |
| 驗收證據            | extension 2634 例、pwa 1081 例全綠；4 個新測試各自 mutation check 轉紅症狀正確                                        |

## 2. 卡點與摩擦

- **G1（529 風暴，僅 subagent 啟動受影響）**：連續 5 次 tester 派工被 `529 Overloaded` 於啟動即殺，主迴圈全程正常；跨約 75 分鐘，最後於長退避後成功。全有或全無——每次陣亡都零編輯，重派 verbatim 即可。退避階梯（3→10→30→45 分）事後看太密：前三次重試全數浪費。
- **G2（reviewer stall → 立即重派成功）**：read-only agent 死亡不需磁碟取證，直接重派即可——與前次 run tester stall 後需 `git status` 核對形成對照，兩者處置準則不同值得固化（見 L2）。
- **U#（使用者決策，非修正）**：reviewer 建議 S1 另開 PR，使用者裁定併入本 PR——「同一缺陷類收齊在一個 PR」勝過「最小 diff」，是本專案的偏好訊號。
- **正向**：mutation check 已成為本輪所有 guard/cleanup 類測試的標配（4/4），兩次都抓到正確的 pre-fix 症狀；chip → in-session 接手流程順暢（chip 因 app 重啟失效無法 dismiss，屬平台限制）。

## 3. 改進提案

- **L0825_1124-1**：`target=.claude/rules/test.md | sketch=Anti-Drift Rules 補一條：pin「不會發生某事」的測試（cleanup/guard 類）必須於撰寫當下做 mutation check（暫時移除受測防護 → 測試須轉紅 → 還原並以 hash/diff 驗證）並在回報中附證據，否則視為空測 | status=proposed`
- **L0825_1124-2**：`target=.claude/skills/develop/SKILL.md | sketch=agent 死亡處置細分：read-only agent（reviewer/security/Explore）可立即重派免取證；會寫檔的 agent（coder/tester）重派前必先 git status 核對磁碟完成度。連續 2 次 529 後改單次長退避（≥30 分），不用密集階梯 | status=proposed`
- **E0825_1124-1**：529 只打 subagent 啟動時，主迴圈可先做不派工的活（本地驗證、diff 自查、報告草稿），把等待期變成產出期——本次在退避期間完成了 pwa 全套驗證。

## 4. Skill / Agent 增減建議

- 無增減。L0825_1124-2 若採納，落點擇一（SKILL.md §3 的 agent-death 段），勿與 code-cycle.md 重複。

## 5. 三軸 KPI

```
speed.bottleneck_seconds: 724
speed.serialized_legs: 0
precision.requirement_rounds: 0
precision.critical_count: 0
precision.suggestion_adopted/proposed: 3/3
precision.rework_rt: 0
context.dispatches: 12
context.self_reads: 2
context.top_consumer: 529 陣亡重派（5 次）與 tester 長 transcript
context.cuttable_lever: 連續 529 改單次長退避（見 L0825_1124-2）
meta.patch_proposed: 2
```
